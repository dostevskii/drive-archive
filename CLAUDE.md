# drive-archive

외장하드에 흩어진 자료를 인덱싱해서, 하드가 연결되어 있지 않아도 "어느 하드에 있는지"를 찾을 수 있게 해주는 Windows 11 전용 도구입니다.

## 사용자 질문에 답하는 법

사용자가 자기 자료의 위치를 물으면 — "그 프로젝트 어느 하드에 있어?", "작년 영상 파일 찾아줘", "이 파일 어디 있지?" — 이 도구로 인덱스를 조회해서 답하세요. 기억이나 추측으로 답하지 마세요.

**MCP가 연결되어 있으면** `search_files`, `list_drives`, `get_status` 도구를 쓰세요.

**MCP가 없으면** CLI를 직접 실행하세요. `--json`을 붙이면 파싱하기 좋습니다.

```bash
drive-archive search "키워드" --json
drive-archive search "키워드" --dirs-only --limit 20 --json   # 프로젝트 폴더 단위로
drive-archive search "키워드" --drive "Works D" --json         # 특정 하드 안에서만
drive-archive drives --json                                    # 하드 목록 + 현재 연결 여부
drive-archive status --json                                    # 인덱스 통계
```

### 답변에 반드시 포함할 것

검색 결과의 **`drive_label`이 이 도구의 존재 이유**입니다. 사용자는 그 이름이 적힌 하드를 서랍에서 물리적으로 찾아 연결해야 합니다. 경로만 알려주고 끝내지 마세요.

`list_drives`의 `connected` 필드로 그 하드가 지금 꽂혀 있는지 확인해서, 연결이 필요한지 아니면 바로 열면 되는지 구분해 주세요.

### 검색이 비었을 때

`status`의 `entry_count`가 0이면 아직 아무 하드도 인덱싱되지 않은 것입니다. 검색어를 바꾸라고 하기 전에 이 경우인지 확인하세요. 인덱싱된 하드가 있는데도 결과가 없다면, 그 하드를 한 번도 연결한 적이 없거나 더 짧은 검색어가 필요한 경우입니다.

## 알아둘 것

- **하드 식별은 볼륨 시리얼로 합니다.** 드라이브 문자(`E:`, `F:`)는 연결 순서에 따라 바뀌므로 인덱스에 저장되지 않습니다. 사용자에게 드라이브 문자로 안내하지 마세요. 라벨로 안내하세요.
- **"어느 하드가 무슨 포맷이지?" 같은 질문에도 답할 수 있습니다.** `list_drives`의 `filesystem` 필드를 쓰세요. 값이 `(알 수 없음)`이면 포맷 기록 기능이 생기기 전에 인덱싱된 하드입니다. 다시 연결하면 채워집니다.
- **경로는 드라이브 루트 기준 상대 경로**입니다. `Company Works\2024\최종.psd`처럼 저장됩니다.
- **USB로 연결된 하드만** 인덱싱됩니다. 파일 시스템은 가리지 않습니다 — NTFS, exFAT, FAT32, ReFS 모두 됩니다. 각 하드의 형식은 인덱스에 기록되며 `list_drives`의 `filesystem` 필드에 있습니다. 사용자가 "왜 이 하드가 안 보이지?"라고 하면, 내장 디스크이거나(USB가 아님) Windows가 마운트하지 못하는 형식(HFS+, APFS, ext4 등 — 별도 드라이버 필요)인지 확인하세요.
- **파일 내용은 저장하지 않습니다.** 이름, 경로, 크기, 수정 시각뿐입니다. 파일 안의 내용을 검색할 수는 없습니다.
- **인덱싱은 하드 연결 시 자동으로** 일어납니다 (작업 스케줄러 이벤트 트리거). 사용자에게 수동 실행을 시키기 전에, 방금 연결했다면 잠시 기다리라고 안내하세요.
- **스캔 결과가 미심쩍으면 반영하지 않습니다.** 하드가 스캔 도중 분리됐거나, 읽기 오류가 있으면서 항목 수가 이전의 절반 아래로 줄었으면 인덱스를 이전 상태로 둡니다. `sync` 출력에 "반영하지 않음"이 보이면 그 하드의 인덱스는 **갱신되지 않은 옛 데이터**입니다. 사용자에게 하드를 다시 연결해 `drive-archive scan <드라이브문자>`를 실행하라고 안내하세요.

## 코드를 고칠 때

- `cargo test`로 검증합니다. 테스트는 각 모듈의 `mod tests`에 있습니다.
- `volume.rs`의 Win32 호출은 실제 하드 없이는 테스트할 수 없습니다. 수정했다면 실제 외장하드로 확인해야 합니다.
- `mcp.rs`의 stdout은 JSON-RPC 전용입니다. `println!`을 넣으면 프로토콜이 깨집니다. 진단은 `eprintln!`으로 보내세요.
- 작업 스케줄러 등록(`task.rs`)과 설치(`install.rs`)는 관리자 권한이 필요합니다.

## 구조

| 파일 | 역할 |
|---|---|
| `main.rs` | CLI 진입점 (clap) |
| `volume.rs` | USB NTFS 볼륨 식별 (Win32) |
| `db.rs` | SQLite 스키마, 스캔 diff 반영, 검색 |
| `scan.rs` | 병렬 디렉토리 순회 (jwalk) |
| `sync.rs` | 연결 시 동기화, 단일 인스턴스 잠금, 로그 |
| `task.rs` | 작업 스케줄러 이벤트 트리거 등록 |
| `mcp.rs` | MCP stdio 서버 (JSON-RPC 2.0 직접 구현) |
| `install.rs` | 원커맨드 설치 (스케줄러 + Claude 설정) |
| `elevation.rs` | 관리자 권한 확인, UAC 상승 |

인덱스는 `%LOCALAPPDATA%\drive-archive\index.db`, 로그는 같은 폴더의 `sync.log`에 있습니다.
