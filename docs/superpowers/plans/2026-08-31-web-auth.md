# 외부 접속용 웹 인증 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `drive-archive serve`에 비밀번호 게이트와 세션을 붙이고 화면을 적응형으로 만들어, 터널을 통해 밖에서도 인덱스를 볼 수 있게 한다.

**Architecture:** 서버는 계속 127.0.0.1에만 뜬다. 외부 노출은 앞에 붙는 터널(Cloudflare Tunnel 또는 Tailscale Funnel)이 맡는다. 새 모듈 `auth.rs`가 비밀번호 해시·세션·잠금을 담당하고, `serve.rs`는 요청마다 세션을 확인한다. 비밀번호가 없으면 `serve`가 아예 뜨지 않고, `install`이 비밀번호를 반드시 받으므로 설치를 마친 상태에는 항상 비밀번호가 있다.

**Tech Stack:** Rust 1.95 (MSVC), edition 2024, argon2 0.5 (password-hash feature), windows 0.62 (`Win32_Security_Cryptography` 추가), 표준 라이브러리 `std::net`

**Spec:** `docs/superpowers/specs/2026-08-31-web-auth-design.md`

## Global Constraints

- **Windows 11 전용.** 다른 플랫폼을 고려하지 않는다.
- **주석·문서·사용자 대상 문구는 모두 한국어.** 기존 코드의 문체를 따른다 — 무엇을 하는지가 아니라 왜 그렇게 했는지를 적는다.
- **테스트는 각 모듈의 `mod tests`에 넣는다.** 별도 `tests/` 디렉토리를 만들지 않는다. 테스트 이름은 기존과 같이 한글로 쓴다.
- **바인딩은 `Ipv4Addr::LOCALHOST` 고정.** `--host` 같은 옵션을 만들지 않는다.
- **새로 늘어나는 크레이트는 argon2 계열 12개뿐.** 난수는 `BCryptGenRandom`으로 얻어 크레이트를 늘리지 않는다.
- **숫자는 실측한 값만 쓴다.** 테스트 수, 코드 줄 수, 크레이트 수를 문서에 적을 때 추측하지 않는다.
- **커밋은 황보정님이 요청할 때만 한다.** 각 태스크 끝의 커밋 명령은 적어 두되, 승인 없이 실행하지 않는다.
- **파일 경로를 다루는 함수는 경로를 인자로 받는 형태로 쪼갠다.** 테스트가 실제 `%LOCALAPPDATA%`를 건드리면 안 된다. `install.rs`의 `store_desktop_configs(packages: &Path)`가 이미 쓰는 방식이다.
- **`ExecutionTimeLimit`은 작업마다 다르다.** sync는 `PT4H`, serve는 `PT0S`(무제한). 이걸 틀리면 서버가 4시간 뒤 죽는다.
- **`target/release/drive-archive.exe`는 MCP 서버가 붙잡고 있으면 덮어쓸 수 없다.** 릴리스 빌드 전에 `Get-Process drive-archive | Stop-Process -Force`로 정리한다.

---

## 파일 구조

| 파일 | 책임 | 상태 |
|---|---|---|
| `src/auth.rs` | 비밀번호 해시 저장·검증, 세션 발급·연장, 로그인 실패 잠금, 난수 | 신규 |
| `src/serve.rs` | HTTP 요청 파싱(헤더·쿠키·본문), 라우팅, 인증 검사, `/api/login` | 수정 |
| `src/web/index.html` | PASSWORD 게이트, 적응형 레이아웃, 터치 조작 | 수정 |
| `src/main.rs` | `passwd` 서브커맨드, 숨긴 입력 | 수정 |
| `src/task.rs` | 작업 이름·XML 매개변수화, serve 작업 정의 | 수정 |
| `src/install.rs` | 설치 시 비밀번호 강제, serve 작업 등록·해제 | 수정 |
| `Cargo.toml` | argon2, windows feature | 수정 |

`auth.rs`를 따로 두는 이유는 인증이 웹 서버와 수명이 다르기 때문이다. `passwd` 명령과 `install`도 이 모듈을 쓰는데, 그쪽에는 HTTP가 없다.

---

### Task 1: auth.rs — 난수와 비밀번호

**Files:**
- Create: `src/auth.rs`
- Modify: `Cargo.toml`, `src/main.rs` (모듈 선언 한 줄)

**Interfaces:**
- Consumes: `crate::db::data_dir()` → `Result<PathBuf>`, `%LOCALAPPDATA%\drive-archive`
- Produces:
  - `pub fn random_bytes(n: usize) -> Result<Vec<u8>>`
  - `pub fn set_password_at(dir: &Path, password: &str) -> Result<()>`
  - `pub fn verify_at(dir: &Path, password: &str) -> Result<bool>`
  - `pub fn is_configured_at(dir: &Path) -> bool`
  - `pub fn set_password(password: &str) -> Result<()>` — `data_dir()`를 넘기는 얇은 껍데기
  - `pub fn verify(password: &str) -> Result<bool>`
  - `pub fn is_configured() -> bool`

- [ ] **Step 1: Cargo.toml에 의존성을 더한다**

`[dependencies]`에 한 줄을 더한다.

```toml
argon2 = { version = "0.5", features = ["password-hash"] }
```

`windows` 줄의 features 배열 끝에 `"Win32_Security_Cryptography"`를 더한다.

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`src/auth.rs`를 만들고 아래를 넣는다. 구현은 아직 없다.

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn 난수는_매번_다르다() {
        let a = random_bytes(32).unwrap();
        let b = random_bytes(32).unwrap();
        assert_eq!(a.len(), 32);
        assert_ne!(a, b, "같은 값이 두 번 나왔다면 난수원이 고장 난 것이다");
    }

    #[test]
    fn 저장한_비밀번호로_통과한다() {
        let dir = tempfile::tempdir().unwrap();
        set_password_at(dir.path(), "정확한비밀번호").unwrap();
        assert!(verify_at(dir.path(), "정확한비밀번호").unwrap());
    }

    #[test]
    fn 다른_비밀번호는_막힌다() {
        let dir = tempfile::tempdir().unwrap();
        set_password_at(dir.path(), "정확한비밀번호").unwrap();
        assert!(!verify_at(dir.path(), "틀린비밀번호").unwrap());
        assert!(!verify_at(dir.path(), "").unwrap());
    }

    #[test]
    fn 설정하지_않았으면_통과시키지_않는다() {
        let dir = tempfile::tempdir().unwrap();
        assert!(!is_configured_at(dir.path()));
        // 파일이 없다고 해서 아무나 통과시키면 안 된다.
        assert!(!verify_at(dir.path(), "아무거나").unwrap());
    }

    #[test]
    fn 다시_설정하면_옛_비밀번호는_막힌다() {
        let dir = tempfile::tempdir().unwrap();
        set_password_at(dir.path(), "첫번째비밀번호").unwrap();
        set_password_at(dir.path(), "두번째비밀번호").unwrap();
        assert!(!verify_at(dir.path(), "첫번째비밀번호").unwrap());
        assert!(verify_at(dir.path(), "두번째비밀번호").unwrap());
    }

    #[test]
    fn 같은_비밀번호도_해시가_다르다() {
        // salt가 매번 새로 나와야 한다. 같으면 salt를 안 쓰고 있는 것이다.
        let a = tempfile::tempdir().unwrap();
        let b = tempfile::tempdir().unwrap();
        set_password_at(a.path(), "같은값입니다").unwrap();
        set_password_at(b.path(), "같은값입니다").unwrap();
        let ha = std::fs::read_to_string(a.path().join("auth.json")).unwrap();
        let hb = std::fs::read_to_string(b.path().join("auth.json")).unwrap();
        assert_ne!(ha, hb);
    }
}
```

- [ ] **Step 3: 테스트가 실패하는 것을 확인한다**

Run: `cargo test auth::`
Expected: 컴파일 실패 — `random_bytes`, `set_password_at` 등이 없다

- [ ] **Step 4: 구현한다**

`src/auth.rs`의 `mod tests` 위에 넣는다.

```rust
//! 웹 화면의 비밀번호와 세션.
//!
//! 서버는 127.0.0.1에만 뜨지만, 로컬에서 볼 때도 터널을 거치므로 서버 입장에서
//! 외부 접속과 로컬 접속은 구별되지 않는다. 그래서 접속 주소로 신뢰를 판단하지
//! 않고 모든 요청에 인증을 요구한다.
//!
//! 비밀번호는 인덱스 DB가 아니라 `auth.json`에 따로 둔다. 인덱스는 스캔이 통째로
//! 갈아엎고 `forget`으로 지워지는 데이터라 인증 정보와 수명이 다르다.

use std::path::{Path, PathBuf};

use anyhow::{Context, Result};
use argon2::password_hash::{PasswordHash, PasswordHasher, PasswordVerifier, SaltString};
use argon2::Argon2;

/// 비밀번호 해시가 놓이는 파일 이름.
const AUTH_FILE: &str = "auth.json";

/// 암호학적 난수를 얻는다. salt와 세션 토큰에 쓴다.
///
/// `BCryptGenRandom`을 쓰는 것은 크레이트를 늘리지 않기 위해서다. `windows`
/// 크레이트는 이미 들어와 있어 feature 하나만 켜면 된다.
pub fn random_bytes(n: usize) -> Result<Vec<u8>> {
    use windows::Win32::Security::Cryptography::{
        BCryptGenRandom, BCRYPT_USE_SYSTEM_PREFERRED_RNG,
    };
    let mut buf = vec![0u8; n];
    unsafe { BCryptGenRandom(None, &mut buf, BCRYPT_USE_SYSTEM_PREFERRED_RNG) }
        .ok()
        .context("난수를 얻지 못했습니다")?;
    Ok(buf)
}

fn auth_path(dir: &Path) -> PathBuf {
    dir.join(AUTH_FILE)
}

/// 비밀번호를 argon2id로 해시해 저장한다. 이미 있으면 덮어쓴다.
pub fn set_password_at(dir: &Path, password: &str) -> Result<()> {
    std::fs::create_dir_all(dir)
        .with_context(|| format!("폴더를 만들 수 없습니다: {}", dir.display()))?;

    let salt_bytes = random_bytes(16)?;
    let salt = SaltString::encode_b64(&salt_bytes)
        .map_err(|e| anyhow::anyhow!("salt를 만들 수 없습니다: {e}"))?;
    let hash = Argon2::default()
        .hash_password(password.as_bytes(), &salt)
        .map_err(|e| anyhow::anyhow!("비밀번호를 해시하지 못했습니다: {e}"))?
        .to_string();

    let body = serde_json::json!({ "hash": hash }).to_string();
    std::fs::write(auth_path(dir), body)
        .with_context(|| format!("비밀번호를 저장할 수 없습니다: {}", dir.display()))?;
    Ok(())
}

/// 비밀번호가 맞는지 본다. 설정되어 있지 않으면 거짓이다.
pub fn verify_at(dir: &Path, password: &str) -> Result<bool> {
    let Ok(body) = std::fs::read_to_string(auth_path(dir)) else {
        return Ok(false);
    };
    let stored: serde_json::Value =
        serde_json::from_str(&body).context("auth.json을 읽을 수 없습니다")?;
    let Some(hash) = stored.get("hash").and_then(|h| h.as_str()) else {
        return Ok(false);
    };
    let parsed = PasswordHash::new(hash)
        .map_err(|e| anyhow::anyhow!("저장된 해시가 깨졌습니다: {e}"))?;
    Ok(Argon2::default()
        .verify_password(password.as_bytes(), &parsed)
        .is_ok())
}

/// 비밀번호가 설정되어 있는가.
pub fn is_configured_at(dir: &Path) -> bool {
    std::fs::read_to_string(auth_path(dir))
        .ok()
        .and_then(|b| serde_json::from_str::<serde_json::Value>(&b).ok())
        .and_then(|v| v.get("hash").and_then(|h| h.as_str()).map(str::to_string))
        .is_some_and(|h| !h.is_empty())
}

pub fn set_password(password: &str) -> Result<()> {
    set_password_at(&crate::db::data_dir()?, password)
}

pub fn verify(password: &str) -> Result<bool> {
    verify_at(&crate::db::data_dir()?, password)
}

pub fn is_configured() -> bool {
    crate::db::data_dir().is_ok_and(|d| is_configured_at(&d))
}
```

`src/main.rs`의 모듈 선언 목록 맨 앞에 `mod auth;`를 넣는다 (알파벳 순).

- [ ] **Step 5: 테스트가 통과하는 것을 확인한다**

Run: `cargo test auth::`
Expected: 6개 통과

- [ ] **Step 6: 커밋 (승인 후)**

```bash
git add Cargo.toml Cargo.lock src/auth.rs src/main.rs
git commit -m "웹 화면 비밀번호 저장과 검증"
```

---

### Task 2: auth.rs — 세션

**Files:**
- Modify: `src/auth.rs`

**Interfaces:**
- Consumes: `random_bytes()` (Task 1)
- Produces:
  - `pub const SESSION_SECS: u64 = 86_400`
  - `pub struct Sessions` — `Default` 구현, `serve`가 하나 만들어 `Arc`로 공유
  - `pub fn issue(&self, now: SystemTime) -> Result<String>`
  - `pub fn check(&self, token: &str, now: SystemTime) -> bool` — 유효하면 만료를 24시간 뒤로 밀고 참
  - `pub fn len(&self) -> usize` — 테스트용

시간을 인자로 받는 이유는 만료와 연장을 테스트할 수 있어야 하기 때문이다. 호출부는 `SystemTime::now()`를 넘긴다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/auth.rs`의 `mod tests`에 더한다.

```rust
    use std::time::Duration;

    #[test]
    fn 발급한_토큰은_통과한다() {
        let s = Sessions::default();
        let now = SystemTime::UNIX_EPOCH;
        let t = s.issue(now).unwrap();
        assert!(s.check(&t, now));
    }

    #[test]
    fn 모르는_토큰은_막는다() {
        let s = Sessions::default();
        let now = SystemTime::UNIX_EPOCH;
        s.issue(now).unwrap();
        assert!(!s.check("아무거나", now));
        assert!(!s.check("", now));
    }

    #[test]
    fn 하루가_지나면_만료된다() {
        let s = Sessions::default();
        let now = SystemTime::UNIX_EPOCH;
        let t = s.issue(now).unwrap();
        let 하루뒤 = now + Duration::from_secs(SESSION_SECS + 1);
        assert!(!s.check(&t, 하루뒤));
    }

    #[test]
    fn 쓰는_동안_만료가_밀린다() {
        // 23시간마다 한 번씩 들어오면 사흘이 지나도 살아 있어야 한다.
        let s = Sessions::default();
        let mut now = SystemTime::UNIX_EPOCH;
        let t = s.issue(now).unwrap();
        for _ in 0..3 {
            now += Duration::from_secs(23 * 3600);
            assert!(s.check(&t, now), "23시간 간격이면 연장되어야 한다");
        }
        // 그러다 하루를 통째로 쉬면 끊긴다.
        now += Duration::from_secs(SESSION_SECS + 1);
        assert!(!s.check(&t, now));
    }

    #[test]
    fn 토큰은_매번_다르다() {
        let s = Sessions::default();
        let now = SystemTime::UNIX_EPOCH;
        let a = s.issue(now).unwrap();
        let b = s.issue(now).unwrap();
        assert_ne!(a, b);
        assert!(a.len() >= 32, "토큰이 짧으면 찍어 맞힐 수 있다");
    }

    #[test]
    fn 만료된_토큰은_보관하지_않는다() {
        // 세션이 쌓이기만 하면 오래 켜 둔 컴퓨터에서 메모리가 는다.
        let s = Sessions::default();
        let now = SystemTime::UNIX_EPOCH;
        let t = s.issue(now).unwrap();
        let 하루뒤 = now + Duration::from_secs(SESSION_SECS + 1);
        s.check(&t, 하루뒤);
        assert_eq!(s.len(), 0);
    }
```

- [ ] **Step 2: 테스트가 실패하는 것을 확인한다**

Run: `cargo test auth::`
Expected: 컴파일 실패 — `Sessions`가 없다

- [ ] **Step 3: 구현한다**

`src/auth.rs`에 더한다. `use` 목록에 `std::collections::HashMap`, `std::sync::Mutex`, `std::time::{Duration, SystemTime}`를 추가한다.

```rust
/// 세션이 살아 있는 시간. 쓰는 동안에는 계속 이만큼씩 밀린다.
pub const SESSION_SECS: u64 = 86_400;

/// 발급한 세션과 각각의 만료 시각.
///
/// 메모리에만 둔다. 서버를 다시 띄우면 전부 무효가 되는데, 단순하고 오히려 안전하다.
#[derive(Default)]
pub struct Sessions {
    live: Mutex<HashMap<String, SystemTime>>,
}

impl Sessions {
    /// 새 토큰을 발급한다. 32바이트 난수를 16진수로 적는다.
    pub fn issue(&self, now: SystemTime) -> Result<String> {
        let token = random_bytes(32)?
            .iter()
            .map(|b| format!("{b:02x}"))
            .collect::<String>();
        let mut live = self.live.lock().unwrap();
        live.insert(token.clone(), now + Duration::from_secs(SESSION_SECS));
        Ok(token)
    }

    /// 유효한 토큰인가. 유효하면 만료를 24시간 뒤로 다시 잡는다.
    ///
    /// 만료된 것은 이 자리에서 지운다. 따로 청소하는 사람이 없으면 오래 켜 둔
    /// 컴퓨터에서 죽은 세션이 쌓이기만 한다.
    pub fn check(&self, token: &str, now: SystemTime) -> bool {
        if token.is_empty() {
            return false;
        }
        let mut live = self.live.lock().unwrap();
        live.retain(|_, expires| *expires > now);
        match live.get_mut(token) {
            Some(expires) => {
                *expires = now + Duration::from_secs(SESSION_SECS);
                true
            }
            None => false,
        }
    }

    /// 살아 있는 세션 수. 테스트에서 쓴다.
    pub fn len(&self) -> usize {
        self.live.lock().unwrap().len()
    }
}
```

- [ ] **Step 4: 테스트가 통과하는 것을 확인한다**

Run: `cargo test auth::`
Expected: 12개 통과 (Task 1의 6개 + 6개)

- [ ] **Step 5: 커밋 (승인 후)**

```bash
git add src/auth.rs
git commit -m "24시간 세션, 쓰는 동안 만료를 미룬다"
```

---

### Task 3: auth.rs — 로그인 실패 잠금

**Files:**
- Modify: `src/auth.rs`

**Interfaces:**
- Produces:
  - `pub struct Gate` — `Default` 구현
  - `pub fn note_failure(&self, ip: &str, now: SystemTime)`
  - `pub fn note_success(&self, ip: &str)`
  - `pub fn locked_for(&self, ip: &str, now: SystemTime) -> Option<u64>` — 잠겨 있으면 남은 초
  - 상수 `IP_LIMIT = 5`, `GLOBAL_LIMIT = 20`, `LOCK_SECS = 60` (테스트가 참조하므로 모듈 안에서 보여야 한다)

**왜 전역 카운터가 같이 필요한가:** IP는 `X-Forwarded-For`에서 읽는데, 이 헤더는 요청을 보내는 쪽이 마음대로 적을 수 있다. 매번 다른 값을 넣으면 IP별 카운터가 매번 새것이 되어 잠금을 통째로 우회한다. 헤더로 갈라지지 않는 전역 카운터가 그 구멍을 막는다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```rust
    #[test]
    fn 다섯_번_틀리면_그_주소가_잠긴다() {
        let g = Gate::default();
        let now = SystemTime::UNIX_EPOCH;
        for _ in 0..4 {
            g.note_failure("1.1.1.1", now);
        }
        assert_eq!(g.locked_for("1.1.1.1", now), None, "4회까지는 열려 있다");
        g.note_failure("1.1.1.1", now);
        assert_eq!(g.locked_for("1.1.1.1", now), Some(LOCK_SECS));
    }

    #[test]
    fn 잠금은_시간이_지나면_풀린다() {
        let g = Gate::default();
        let now = SystemTime::UNIX_EPOCH;
        for _ in 0..5 {
            g.note_failure("1.1.1.1", now);
        }
        let 나중 = now + Duration::from_secs(LOCK_SECS + 1);
        assert_eq!(g.locked_for("1.1.1.1", 나중), None);
    }

    #[test]
    fn 성공하면_카운터가_지워진다() {
        let g = Gate::default();
        let now = SystemTime::UNIX_EPOCH;
        for _ in 0..4 {
            g.note_failure("1.1.1.1", now);
        }
        g.note_success("1.1.1.1");
        for _ in 0..4 {
            g.note_failure("1.1.1.1", now);
        }
        assert_eq!(g.locked_for("1.1.1.1", now), None, "성공이 카운터를 지웠어야 한다");
    }

    #[test]
    fn 다른_주소는_말려들지_않는다() {
        let g = Gate::default();
        let now = SystemTime::UNIX_EPOCH;
        for _ in 0..5 {
            g.note_failure("1.1.1.1", now);
        }
        assert!(g.locked_for("1.1.1.1", now).is_some());
        assert_eq!(g.locked_for("2.2.2.2", now), None);
    }

    #[test]
    fn 주소를_매번_바꿔도_전역_카운터에_걸린다() {
        // X-Forwarded-For는 보내는 쪽이 지어낼 수 있다. 주소별 카운터만 두면
        // 매 요청마다 다른 값을 적어 잠금을 통째로 피해 간다.
        let g = Gate::default();
        let now = SystemTime::UNIX_EPOCH;
        for i in 0..GLOBAL_LIMIT {
            g.note_failure(&format!("10.0.0.{i}"), now);
        }
        assert_eq!(
            g.locked_for("처음보는주소", now),
            Some(LOCK_SECS),
            "전역 카운터가 한도에 닿으면 처음 보는 주소도 막아야 한다"
        );
    }

    #[test]
    fn 전역_잠금도_시간이_지나면_풀린다() {
        let g = Gate::default();
        let now = SystemTime::UNIX_EPOCH;
        for i in 0..GLOBAL_LIMIT {
            g.note_failure(&format!("10.0.0.{i}"), now);
        }
        let 나중 = now + Duration::from_secs(LOCK_SECS + 1);
        assert_eq!(g.locked_for("처음보는주소", 나중), None);
    }
```

- [ ] **Step 2: 테스트가 실패하는 것을 확인한다**

Run: `cargo test auth::`
Expected: 컴파일 실패 — `Gate`가 없다

- [ ] **Step 3: 구현한다**

```rust
/// 한 주소가 이만큼 틀리면 잠근다.
const IP_LIMIT: u32 = 5;
/// 주소를 가리지 않고 이만큼 쌓이면 전부 잠근다.
const GLOBAL_LIMIT: u32 = 20;
/// 잠가 두는 시간.
const LOCK_SECS: u64 = 60;

/// 로그인 실패를 세어 무차별 대입을 막는다.
///
/// argon2 검증 자체가 느려 초당 수십 회가 한계지만, 밖에 열리는 이상 잠금이 있어야 한다.
#[derive(Default)]
pub struct Gate {
    /// 주소별 (실패 횟수, 마지막 실패 시각)
    per_ip: Mutex<HashMap<String, (u32, SystemTime)>>,
    /// 주소를 가리지 않은 (실패 횟수, 마지막 실패 시각).
    /// `X-Forwarded-For`를 지어내 주소별 카운터를 피해 가는 경우를 막는다.
    global: Mutex<Option<(u32, SystemTime)>>,
}

/// 마지막 실패로부터 얼마 지났는지 보고, 잠금 시간이 남았으면 그 초를 준다.
fn lock_remaining(at: SystemTime, now: SystemTime) -> Option<u64> {
    let passed = now.duration_since(at).unwrap_or_default().as_secs();
    (passed <= LOCK_SECS).then(|| LOCK_SECS - passed)
}

impl Gate {
    pub fn note_failure(&self, ip: &str, now: SystemTime) {
        {
            let mut per_ip = self.per_ip.lock().unwrap();
            let e = per_ip.entry(ip.to_string()).or_insert((0, now));
            // 잠금 시간이 지났으면 처음부터 다시 센다.
            if lock_remaining(e.1, now).is_none() {
                *e = (0, now);
            }
            e.0 += 1;
            e.1 = now;
        }

        let mut g = self.global.lock().unwrap();
        let cur = match *g {
            Some((n, at)) if lock_remaining(at, now).is_some() => n,
            _ => 0,
        };
        *g = Some((cur + 1, now));
    }

    pub fn note_success(&self, ip: &str) {
        self.per_ip.lock().unwrap().remove(ip);
    }

    /// 잠겨 있으면 남은 초를 준다.
    pub fn locked_for(&self, ip: &str, now: SystemTime) -> Option<u64> {
        if let Some((n, at)) = *self.global.lock().unwrap() {
            if n >= GLOBAL_LIMIT {
                if let Some(left) = lock_remaining(at, now) {
                    return Some(left);
                }
            }
        }

        let per_ip = self.per_ip.lock().unwrap();
        let &(n, at) = per_ip.get(ip)?;
        if n < IP_LIMIT {
            return None;
        }
        lock_remaining(at, now)
    }
}
```

- [ ] **Step 4: 테스트가 통과하는 것을 확인한다**

Run: `cargo test auth::`
Expected: 18개 통과

- [ ] **Step 5: 커밋 (승인 후)**

```bash
git add src/auth.rs
git commit -m "로그인 실패 잠금 — 주소별과 전역 두 겹"
```

---

### Task 4: passwd 명령과 숨긴 입력

**Files:**
- Modify: `src/main.rs`

**Interfaces:**
- Consumes: `auth::set_password()`, `auth::is_configured()` (Task 1)
- Produces:
  - `fn check_new_password(first: &str, second: &str) -> Result<String>`
  - `pub fn read_hidden(prompt: &str) -> Result<String>`
  - `pub fn prompt_and_set_password() -> Result<()>` — `install.rs`(Task 11)가 쓴다
  - `Cmd::Passwd` 서브커맨드

- [ ] **Step 1: 실패하는 테스트를 쓴다**

콘솔 입력 자체는 자동으로 검증할 수 없다. 검증할 수 있는 것은 입력값 판정 규칙이라, 그 부분을 함수로 떼어 테스트한다. `src/main.rs`의 `mod tests`에 더한다.

```rust
    #[test]
    fn 빈_비밀번호는_거부한다() {
        assert!(check_new_password("", "").is_err());
        assert!(check_new_password("        ", "        ").is_err());
    }

    #[test]
    fn 두_입력이_다르면_거부한다() {
        assert!(check_new_password("충분히긴비밀번호", "충분히긴비밀번호다").is_err());
    }

    #[test]
    fn 너무_짧으면_거부한다() {
        // 밖에 열리는 화면이다. 네 글자짜리는 찍어서 뚫린다.
        assert!(check_new_password("1234", "1234").is_err());
        assert!(check_new_password("일곱글자짜리요", "일곱글자짜리요").is_err());
    }

    #[test]
    fn 쓸_만한_비밀번호는_통과한다() {
        assert!(check_new_password("충분히긴비밀번호", "충분히긴비밀번호").is_ok());
        // 앞뒤 공백은 사용자가 의도한 것일 수 있으므로 지우지 않는다.
        assert_eq!(check_new_password(" abcdefgh", " abcdefgh").unwrap(), " abcdefgh");
    }
```

- [ ] **Step 2: 테스트가 실패하는 것을 확인한다**

Run: `cargo test`
Expected: 컴파일 실패 — `check_new_password`가 없다

- [ ] **Step 3: 구현한다**

`src/main.rs`에 더한다.

```rust
/// 새 비밀번호로 쓸 수 있는지 본다. 통과하면 그 값을 돌려준다.
///
/// 앞뒤 공백을 지우지 않는 것은 사용자가 일부러 넣었을 수 있어서다. 다만 공백만
/// 있는 것은 실수로 본다.
fn check_new_password(first: &str, second: &str) -> Result<String> {
    if first != second {
        anyhow::bail!("두 번 입력한 값이 다릅니다.");
    }
    if first.trim().is_empty() {
        anyhow::bail!("비밀번호가 비어 있습니다.");
    }
    if first.chars().count() < 8 {
        anyhow::bail!("비밀번호는 8자 이상이어야 합니다. 밖에서 접속할 수 있는 화면입니다.");
    }
    Ok(first.to_string())
}

/// 화면에 찍히지 않게 한 줄을 받는다.
///
/// 별표도 찍지 않는다. 터미널에서는 그것이 관례이고, 길이가 어깨너머로 새지 않는다.
/// 파이프로 넘어온 입력에는 콘솔 모드가 없으므로 그때는 그냥 읽는다.
fn read_hidden(prompt: &str) -> Result<String> {
    use std::io::{BufRead, Write};
    use windows::Win32::System::Console::{
        GetConsoleMode, GetStdHandle, SetConsoleMode, CONSOLE_MODE, ENABLE_ECHO_INPUT,
        STD_INPUT_HANDLE,
    };

    print!("{prompt}");
    std::io::stdout().flush().ok();

    let handle = unsafe { GetStdHandle(STD_INPUT_HANDLE) }.context("표준 입력을 열 수 없습니다")?;
    let mut mode = CONSOLE_MODE::default();
    let is_console = unsafe { GetConsoleMode(handle, &mut mode) }.is_ok();
    if is_console {
        let _ = unsafe { SetConsoleMode(handle, mode & !ENABLE_ECHO_INPUT) };
    }

    let mut line = String::new();
    let read = std::io::stdin().lock().read_line(&mut line);

    if is_console {
        let _ = unsafe { SetConsoleMode(handle, mode) };
        println!();   // 사용자가 누른 Enter가 화면에 남지 않았으므로 줄을 바꿔 준다
    }
    read.context("입력을 읽을 수 없습니다")?;
    Ok(line.trim_end_matches(['\r', '\n']).to_string())
}

/// 새 비밀번호를 두 번 받아 저장한다. `install`도 이 함수를 쓴다.
fn prompt_and_set_password() -> Result<()> {
    let first = read_hidden("새 비밀번호 (8자 이상): ")?;
    let second = read_hidden("한 번 더: ")?;
    let password = check_new_password(&first, &second)?;
    auth::set_password(&password)?;
    Ok(())
}

fn cmd_passwd() -> Result<()> {
    if auth::is_configured() {
        println!("이미 설정된 비밀번호를 새 것으로 바꿉니다.");
    } else {
        println!("웹 화면에서 쓸 비밀번호를 설정합니다.");
    }
    prompt_and_set_password()?;
    println!("비밀번호를 저장했습니다.");
    println!("이미 열려 있는 웹 세션은 그대로 살아 있습니다. 모두 끊으려면 서버를 다시 시작하세요.");
    Ok(())
}
```

`Cmd`에 더한다. `Serve` 바로 뒤에 놓는다.

```rust
    /// 웹 화면 비밀번호를 설정하거나 바꿉니다
    Passwd,
```

`run()`의 match에 더한다.

```rust
        Cmd::Passwd => cmd_passwd(),
```

`install.rs`는 `crate::prompt_and_set_password()`로 부른다. 크레이트 루트의 비공개
항목은 하위 모듈 전부에서 보이므로 `pub`을 붙일 필요가 없다.

- [ ] **Step 4: 테스트가 통과하는 것을 확인한다**

Run: `cargo test`
Expected: Task 3까지의 테스트 + 4개 통과

- [ ] **Step 5: 손으로 확인한다**

Run: `cargo run -- passwd`

Expected:
- 입력한 글자가 화면에 찍히지 않는다
- 두 번 다르게 치면 "두 번 입력한 값이 다릅니다."
- 8자 미만이면 "비밀번호는 8자 이상이어야 합니다."
- 성공하면 `%LOCALAPPDATA%\drive-archive\auth.json`이 생긴다

**이 단계에서 검증용 임시 비밀번호를 정한다.** 실사용 비밀번호를 쓰지 않는다. 작업이 모두 끝난 뒤 황보정님이 직접 `drive-archive passwd`로 바꾸신다.

- [ ] **Step 6: 커밋 (승인 후)**

```bash
git add src/main.rs
git commit -m "passwd 명령 — 터미널에서만 비밀번호를 정한다"
```

---

### Task 5: serve.rs — 요청 파싱

**Files:**
- Modify: `src/serve.rs`

**Interfaces:**
- Produces:
  - `pub struct Request { method, path, query, headers, body, peer }`
  - `fn read_request(stream: &mut TcpStream, peer: String) -> Result<Option<Request>>`
  - `impl Request`: `header(&self, name: &str) -> Option<&str>`, `cookie(&self, name: &str) -> Option<&str>`, `client_ip(&self) -> String`, `is_https(&self) -> bool`
  - `const MAX_BODY: usize = 8192`

지금 `read_request_target`은 GET만 받고 헤더를 통째로 버린다. 쿠키를 읽어야 하고 `/api/login`이 본문을 받아야 하므로 요청 전체를 구조체로 만든다. 헤더 이름은 소문자로 눕혀 보관한다 — HTTP 헤더 이름은 대소문자를 가리지 않는데, 프록시마다 다르게 적어 보낸다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/serve.rs`의 `mod tests`에 더한다.

```rust
    /// 테스트용 요청을 하나 만든다.
    fn req(raw: &str) -> Option<Request> {
        let listener = TcpListener::bind((Ipv4Addr::LOCALHOST, 0)).unwrap();
        let port = listener.local_addr().unwrap().port();
        let raw = raw.to_string();
        std::thread::spawn(move || {
            let mut s = TcpStream::connect((Ipv4Addr::LOCALHOST, port)).unwrap();
            let _ = s.write_all(raw.as_bytes());
            let _ = s.flush();
            // 서버가 읽을 때까지 붙들고 있는다. 먼저 닫으면 본문이 잘린다.
            std::thread::sleep(std::time::Duration::from_millis(200));
        });
        let (mut stream, _) = listener.accept().unwrap();
        read_request(&mut stream, "1.2.3.4".to_string()).unwrap()
    }

    #[test]
    fn 경로와_쿼리를_나눠_읽는다() {
        let r = req("GET /api/search?q=%ED%95%9C HTTP/1.1\r\nHost: x\r\n\r\n").unwrap();
        assert_eq!(r.method, "GET");
        assert_eq!(r.path, "/api/search");
        assert_eq!(r.query, "q=%ED%95%9C");
    }

    #[test]
    fn 쿼리가_없으면_빈_문자열이다() {
        let r = req("GET / HTTP/1.1\r\nHost: x\r\n\r\n").unwrap();
        assert_eq!(r.path, "/");
        assert_eq!(r.query, "");
    }

    #[test]
    fn 헤더_이름은_대소문자를_가리지_않는다() {
        let r = req("GET / HTTP/1.1\r\nX-Forwarded-Proto: https\r\n\r\n").unwrap();
        assert_eq!(r.header("x-forwarded-proto"), Some("https"));
        assert_eq!(r.header("X-Forwarded-Proto"), Some("https"));
        assert!(r.is_https());
    }

    #[test]
    fn 프로토콜_헤더가_없으면_평문으로_본다() {
        let r = req("GET / HTTP/1.1\r\nHost: x\r\n\r\n").unwrap();
        assert!(!r.is_https());
    }

    #[test]
    fn 쿠키를_이름으로_꺼낸다() {
        let r = req("GET / HTTP/1.1\r\nCookie: other=1; da=abc123; last=z\r\n\r\n").unwrap();
        assert_eq!(r.cookie("da"), Some("abc123"));
        assert_eq!(r.cookie("other"), Some("1"));
        assert_eq!(r.cookie("last"), Some("z"));
        assert_eq!(r.cookie("없는것"), None);
    }

    #[test]
    fn 쿠키가_없어도_터지지_않는다() {
        let r = req("GET / HTTP/1.1\r\nHost: x\r\n\r\n").unwrap();
        assert_eq!(r.cookie("da"), None);
        let r = req("GET / HTTP/1.1\r\nCookie: \r\n\r\n").unwrap();
        assert_eq!(r.cookie("da"), None);
    }

    #[test]
    fn 포스트_본문을_읽는다() {
        let body = r#"{"password":"열려라참깨"}"#;
        let raw = format!(
            "POST /api/login HTTP/1.1\r\nContent-Length: {}\r\n\r\n{body}",
            body.len()
        );
        let r = req(&raw).unwrap();
        assert_eq!(r.method, "POST");
        assert_eq!(String::from_utf8_lossy(&r.body), body);
    }

    #[test]
    fn 본문이_너무_크면_받지_않는다() {
        // 로그인 본문은 몇십 바이트다. 큰 것을 받아 줄 이유가 없다.
        let raw = format!(
            "POST /api/login HTTP/1.1\r\nContent-Length: {}\r\n\r\n",
            MAX_BODY + 1
        );
        assert!(req(&raw).is_none());
    }

    #[test]
    fn 헤더를_한없이_받아_주지_않는다() {
        // 터널을 거쳐 아무나 보낼 수 있는 자리다. 헤더를 끝없이 흘리면
        // 요청마다 메모리가 그만큼 자란다.
        let raw = format!("GET / HTTP/1.1\r\n{}\r\n", "X-Filler: 채움\r\n".repeat(2000));
        assert!(req(&raw).is_none());
    }

    #[test]
    fn 전달받은_주소를_쓰고_없으면_소켓_주소를_쓴다() {
        let r = req("GET / HTTP/1.1\r\nX-Forwarded-For: 9.9.9.9, 8.8.8.8\r\n\r\n").unwrap();
        assert_eq!(r.client_ip(), "9.9.9.9", "맨 앞이 원래 보낸 쪽이다");
        let r = req("GET / HTTP/1.1\r\nHost: x\r\n\r\n").unwrap();
        assert_eq!(r.client_ip(), "1.2.3.4");
    }
```

- [ ] **Step 2: 테스트가 실패하는 것을 확인한다**

Run: `cargo test serve::`
Expected: 컴파일 실패 — `Request`, `read_request`가 없다

- [ ] **Step 3: 구현한다**

`src/serve.rs`에서 `read_request_target`을 지우고 아래로 갈아낀다. `use std::io::Read`를 파일 위쪽 `use`에 더한다.

```rust
/// 받아 줄 본문의 최대 크기. 로그인 본문은 몇십 바이트라 넉넉히 잡아도 이 정도다.
const MAX_BODY: usize = 8192;

/// 받아 줄 헤더 전체의 최대 크기. 브라우저 요청은 1KB 안팎이다.
const MAX_HEADERS: usize = 16 * 1024;

/// 요청 하나.
///
/// 헤더 이름은 소문자로 눕혀 보관한다. HTTP 헤더 이름은 대소문자를 가리지 않는데
/// 프록시마다 다르게 적어 보낸다.
pub struct Request {
    pub method: String,
    pub path: String,
    pub query: String,
    headers: Vec<(String, String)>,
    pub body: Vec<u8>,
    /// 소켓 상대 주소. 터널 뒤에서는 전부 127.0.0.1로 보인다.
    peer: String,
}

impl Request {
    pub fn header(&self, name: &str) -> Option<&str> {
        let name = name.to_ascii_lowercase();
        self.headers
            .iter()
            .find(|(k, _)| *k == name)
            .map(|(_, v)| v.as_str())
    }

    /// 쿠키 하나를 이름으로 꺼낸다.
    pub fn cookie(&self, name: &str) -> Option<&str> {
        let raw = self.header("cookie")?;
        raw.split(';').find_map(|pair| {
            let (k, v) = pair.split_once('=')?;
            (k.trim() == name).then(|| v.trim())
        })
    }

    /// 로그인 실패를 셀 때 쓸 주소.
    ///
    /// 터널을 거치면 소켓 주소가 전부 127.0.0.1이 되어, 그것만 쓰면 한 사람의 오타가
    /// 전체를 잠근다. 다만 이 헤더는 보내는 쪽이 지어낼 수 있으므로 이것만 믿지
    /// 않는다 — `auth::Gate`가 전역 카운터를 함께 센다.
    pub fn client_ip(&self) -> String {
        self.header("x-forwarded-for")
            .and_then(|v| v.split(',').next())
            .map(|s| s.trim().to_string())
            .filter(|s| !s.is_empty())
            .unwrap_or_else(|| self.peer.clone())
    }

    /// 터널이 HTTPS로 받아 넘긴 요청인가. 쿠키에 `Secure`를 붙일지 정하는 데 쓴다.
    pub fn is_https(&self) -> bool {
        self.header("x-forwarded-proto").is_some_and(|v| v.eq_ignore_ascii_case("https"))
    }
}

/// 요청 하나를 끝까지 읽는다. 읽을 수 없는 요청이면 `None`을 준다.
fn read_request(stream: &mut TcpStream, peer: String) -> Result<Option<Request>> {
    // 터널을 거쳐 아무나 닿는 자리다. 한 글자씩 흘리며 스레드를 영영 붙드는
    // 연결은 시간으로 끊고, 전체 요청은 크기로 자른다. `take`가 헤더 한 줄이
    // 한없이 자라는 것까지 막는다.
    let _ = stream.set_read_timeout(Some(std::time::Duration::from_secs(10)));
    let mut reader = BufReader::new(stream.try_clone()?.take(64 * 1024));

    let mut line = String::new();
    if reader.read_line(&mut line)? == 0 {
        return Ok(None);
    }
    let mut parts = line.split_whitespace();
    let method = parts.next().unwrap_or("").to_string();
    let target = parts.next().unwrap_or("/").to_string();

    let mut headers = Vec::new();
    let mut header_bytes = 0usize;
    loop {
        let mut h = String::new();
        if reader.read_line(&mut h)? == 0 || h == "\r\n" || h == "\n" {
            break;
        }
        header_bytes += h.len();
        if header_bytes > MAX_HEADERS {
            return Ok(None);
        }
        if let Some((k, v)) = h.split_once(':') {
            headers.push((k.trim().to_ascii_lowercase(), v.trim().to_string()));
        }
    }

    let len: usize = headers
        .iter()
        .find(|(k, _)| k == "content-length")
        .and_then(|(_, v)| v.parse().ok())
        .unwrap_or(0);
    if len > MAX_BODY {
        return Ok(None);
    }
    let mut body = vec![0u8; len];
    if len > 0 {
        reader.read_exact(&mut body)?;
    }

    let (path, query) = match target.split_once('?') {
        Some((p, q)) => (p.to_string(), q.to_string()),
        None => (target, String::new()),
    };

    Ok(Some(Request { method, path, query, headers, body, peer }))
}
```

`handle`은 다음 태스크에서 손대므로, 지금은 `read_request`를 쓰도록 최소한만 고친다.

```rust
fn handle(mut stream: TcpStream) -> Result<()> {
    let peer = stream
        .peer_addr()
        .map(|a| a.ip().to_string())
        .unwrap_or_else(|_| "?".to_string());
    let Some(req) = read_request(&mut stream, peer)? else {
        return Ok(());
    };
    if req.method != "GET" {
        return respond(&mut stream, 404, "text/plain; charset=utf-8", b"not found");
    }
    let (path, query) = (req.path.as_str(), req.query.as_str());
    // ...아래 match는 그대로...
}
```

- [ ] **Step 4: 테스트가 통과하는 것을 확인한다**

Run: `cargo test serve::`
Expected: 기존 5개 + 새 10개 통과

- [ ] **Step 5: 커밋 (승인 후)**

```bash
git add src/serve.rs
git commit -m "요청을 구조체로 읽는다 — 헤더·쿠키·본문"
```

---

### Task 6: serve.rs — 로그인과 인증 검사

**Files:**
- Modify: `src/serve.rs`

**Interfaces:**
- Consumes: `auth::{Sessions, Gate, verify_at, is_configured, SESSION_SECS}` (Task 1-3), `Request` (Task 5)
- Produces:
  - `pub struct ServeState { dir: PathBuf, sessions: Sessions, gate: Gate }`
  - `fn api_login(req: &Request, st: &ServeState) -> (u16, String, Option<String>)` — (상태, 본문, Set-Cookie)
  - `fn respond_with(stream, status, content_type, body, extra: &[String]) -> Result<()>`
  - `const COOKIE_NAME: &str = "da"`

`ServeState`가 `dir`을 들고 있는 이유는 테스트가 실제 `%LOCALAPPDATA%`를 건드리지 않게 하기 위해서다. 실행할 때는 `db::data_dir()`을 넣는다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```rust
    /// 비밀번호가 설정된 임시 상태를 만든다.
    fn 준비된_상태(pw: &str) -> (tempfile::TempDir, ServeState) {
        let dir = tempfile::tempdir().unwrap();
        crate::auth::set_password_at(dir.path(), pw).unwrap();
        let st = ServeState {
            dir: dir.path().to_path_buf(),
            sessions: Default::default(),
            gate: Default::default(),
        };
        (dir, st)
    }

    fn 로그인_요청(pw: &str) -> Request {
        let body = serde_json::json!({ "password": pw }).to_string();
        let raw = format!(
            "POST /api/login HTTP/1.1\r\nContent-Length: {}\r\n\r\n{body}",
            body.len()
        );
        req(&raw).unwrap()
    }

    #[test]
    fn 맞는_비밀번호는_쿠키를_받는다() {
        let (_d, st) = 준비된_상태("열려라참깨입니다");
        let (status, _body, cookie) = api_login(&로그인_요청("열려라참깨입니다"), &st);
        assert_eq!(status, 200);
        let cookie = cookie.expect("쿠키가 있어야 한다");
        assert!(cookie.contains("da="));
        assert!(cookie.contains("HttpOnly"), "스크립트가 읽으면 안 된다");
        assert!(cookie.contains("SameSite=Strict"));
        assert!(cookie.contains("Max-Age=86400"));
    }

    #[test]
    fn 틀린_비밀번호는_막힌다() {
        let (_d, st) = 준비된_상태("열려라참깨입니다");
        let (status, _body, cookie) = api_login(&로그인_요청("닫혀라참깨입니다"), &st);
        assert_eq!(status, 401);
        assert!(cookie.is_none());
    }

    #[test]
    fn 다섯_번_틀리면_잠긴다() {
        let (_d, st) = 준비된_상태("열려라참깨입니다");
        for _ in 0..5 {
            api_login(&로그인_요청("틀린비밀번호입니다"), &st);
        }
        // 이제는 맞는 비밀번호를 넣어도 잠금이 먼저다.
        let (status, body, _) = api_login(&로그인_요청("열려라참깨입니다"), &st);
        assert_eq!(status, 429);
        assert!(body.contains("locked"), "{body}");
    }

    #[test]
    fn 평문_접속에는_시큐어를_붙이지_않는다() {
        // http://127.0.0.1로 직접 볼 때 Secure를 붙이면 쿠키가 저장되지 않는다.
        let (_d, st) = 준비된_상태("열려라참깨입니다");
        let (_, _, cookie) = api_login(&로그인_요청("열려라참깨입니다"), &st);
        assert!(!cookie.unwrap().contains("Secure"));
    }

    #[test]
    fn 터널을_거친_접속에는_시큐어를_붙인다() {
        let (_d, st) = 준비된_상태("열려라참깨입니다");
        let body = serde_json::json!({ "password": "열려라참깨입니다" }).to_string();
        let raw = format!(
            "POST /api/login HTTP/1.1\r\nX-Forwarded-Proto: https\r\nContent-Length: {}\r\n\r\n{body}",
            body.len()
        );
        let (_, _, cookie) = api_login(&req(&raw).unwrap(), &st);
        assert!(cookie.unwrap().contains("Secure"));
    }

    #[test]
    fn 세션_없이는_인덱스를_볼_수_없다() {
        let (_d, st) = 준비된_상태("열려라참깨입니다");
        let r = req("GET /api/drives HTTP/1.1\r\nHost: x\r\n\r\n").unwrap();
        assert!(!authed(&r, &st));
    }

    #[test]
    fn 발급받은_쿠키로는_볼_수_있다() {
        let (_d, st) = 준비된_상태("열려라참깨입니다");
        let (_, _, cookie) = api_login(&로그인_요청("열려라참깨입니다"), &st);
        let token = cookie
            .unwrap()
            .trim_start_matches("Set-Cookie: da=")
            .split(';')
            .next()
            .unwrap()
            .to_string();
        let raw = format!("GET /api/drives HTTP/1.1\r\nCookie: da={token}\r\n\r\n");
        assert!(authed(&req(&raw).unwrap(), &st));
    }

    #[test]
    fn 화면과_폰트는_로그인_없이_내려준다() {
        // 게이트 화면 자체가 이 HTML이다. 여기에는 인덱스 내용이 들어 있지 않다.
        assert!(!needs_auth("/"));
        assert!(!needs_auth("/index.html"));
        assert!(!needs_auth("/font.woff2"));
        assert!(!needs_auth("/api/login"));
        assert!(needs_auth("/api/drives"));
        assert!(needs_auth("/api/search"));
        assert!(needs_auth("/api/list"));
    }
```

- [ ] **Step 2: 테스트가 실패하는 것을 확인한다**

Run: `cargo test serve::`
Expected: 컴파일 실패 — `ServeState`, `api_login`, `authed`, `needs_auth`가 없다

- [ ] **Step 3: 구현한다**

```rust
use std::path::PathBuf;
use std::sync::Arc;
use std::time::SystemTime;

use crate::auth::{Gate, Sessions, SESSION_SECS};

/// 세션 쿠키 이름.
const COOKIE_NAME: &str = "da";

/// 서버가 도는 동안 들고 있는 것.
///
/// `dir`을 밖에서 받는 이유는 테스트가 실제 `%LOCALAPPDATA%`를 건드리지 않게
/// 하기 위해서다. 실행할 때는 `db::data_dir()`이 들어간다.
pub struct ServeState {
    pub dir: PathBuf,
    pub sessions: Sessions,
    pub gate: Gate,
}

/// 로그인 없이 내려줘도 되는 경로인가.
fn needs_auth(path: &str) -> bool {
    !matches!(path, "/" | "/index.html" | "/font.woff2" | "/api/login")
}

/// 쿠키의 세션이 살아 있는가. 살아 있으면 만료가 24시간 뒤로 밀린다.
fn authed(req: &Request, st: &ServeState) -> bool {
    let token = req.cookie(COOKIE_NAME).unwrap_or("");
    st.sessions.check(token, SystemTime::now())
}

/// 세션 쿠키 한 줄을 만든다.
///
/// `Secure`는 터널을 거쳐 들어온 요청에만 붙인다. 로컬에서 `http://127.0.0.1`로
/// 직접 볼 때 붙이면 브라우저가 쿠키를 저장하지 않는다.
fn session_cookie(token: &str, https: bool) -> String {
    let secure = if https { "; Secure" } else { "" };
    format!(
        "Set-Cookie: {COOKIE_NAME}={token}; HttpOnly; SameSite=Strict; Path=/; Max-Age={SESSION_SECS}{secure}"
    )
}

/// 로그인을 처리한다. (상태 코드, 본문, Set-Cookie 한 줄)
fn api_login(req: &Request, st: &ServeState) -> (u16, String, Option<String>) {
    let ip = req.client_ip();
    let now = SystemTime::now();

    if let Some(left) = st.gate.locked_for(&ip, now) {
        let body = serde_json::json!({ "error": "locked", "seconds": left }).to_string();
        return (429, body, None);
    }

    let password = serde_json::from_slice::<serde_json::Value>(&req.body)
        .ok()
        .and_then(|v| v.get("password").and_then(|p| p.as_str()).map(str::to_string))
        .unwrap_or_default();

    match crate::auth::verify_at(&st.dir, &password) {
        Ok(true) => {
            st.gate.note_success(&ip);
            match st.sessions.issue(now) {
                Ok(token) => (
                    200,
                    serde_json::json!({ "ok": true }).to_string(),
                    Some(session_cookie(&token, req.is_https())),
                ),
                Err(e) => (
                    500,
                    serde_json::json!({ "error": format!("{e:#}") }).to_string(),
                    None,
                ),
            }
        }
        Ok(false) => {
            st.gate.note_failure(&ip, now);
            (401, serde_json::json!({ "error": "wrong" }).to_string(), None)
        }
        Err(e) => (
            500,
            serde_json::json!({ "error": format!("{e:#}") }).to_string(),
            None,
        ),
    }
}
```

`respond`를 헤더를 더 실을 수 있게 바꾼다. 기존 `respond`는 얇은 껍데기로 남겨 호출부를 건드리지 않는다.

```rust
fn respond(stream: &mut TcpStream, status: u16, content_type: &str, body: &[u8]) -> Result<()> {
    respond_with(stream, status, content_type, body, &[])
}

fn respond_with(
    stream: &mut TcpStream,
    status: u16,
    content_type: &str,
    body: &[u8],
    extra: &[String],
) -> Result<()> {
    let reason = match status {
        200 => "OK",
        401 => "Unauthorized",
        404 => "Not Found",
        429 => "Too Many Requests",
        _ => "Internal Server Error",
    };
    let mut head = format!(
        "HTTP/1.1 {status} {reason}\r\n\
         Content-Type: {content_type}\r\n\
         Content-Length: {}\r\n\
         Cache-Control: no-store\r\n\
         Connection: close\r\n",
        body.len()
    );
    for line in extra {
        head.push_str(line);
        head.push_str("\r\n");
    }
    head.push_str("\r\n");
    stream.write_all(head.as_bytes())?;
    stream.write_all(body)?;
    stream.flush()?;
    Ok(())
}
```

`handle`을 상태를 받도록 바꾼다.

```rust
fn handle(mut stream: TcpStream, st: &ServeState) -> Result<()> {
    let peer = stream
        .peer_addr()
        .map(|a| a.ip().to_string())
        .unwrap_or_else(|_| "?".to_string());
    let Some(req) = read_request(&mut stream, peer)? else {
        return Ok(());
    };

    if req.path == "/api/login" && req.method == "POST" {
        let (status, body, cookie) = api_login(&req, st);
        let extra: Vec<String> = cookie.into_iter().collect();
        return respond_with(
            &mut stream,
            status,
            "application/json; charset=utf-8",
            body.as_bytes(),
            &extra,
        );
    }

    if req.method != "GET" {
        return respond(&mut stream, 404, "text/plain; charset=utf-8", b"not found");
    }

    // 인덱스를 건드리는 경로는 세션이 있어야 한다. 통과하면 그 자리에서 만료가
    // 24시간 뒤로 밀리므로, 갱신된 쿠키를 응답에 같이 실어 보낸다.
    let mut extra: Vec<String> = Vec::new();
    if needs_auth(&req.path) {
        if !authed(&req, st) {
            let body = serde_json::json!({ "error": "unauthorized" }).to_string();
            return respond(&mut stream, 401, "application/json; charset=utf-8", body.as_bytes());
        }
        if let Some(token) = req.cookie(COOKIE_NAME) {
            extra.push(session_cookie(token, req.is_https()));
        }
    }

    let (path, query) = (req.path.as_str(), req.query.as_str());
    let json = "application/json; charset=utf-8";

    match path {
        "/" | "/index.html" => {
            respond(&mut stream, 200, "text/html; charset=utf-8", INDEX_HTML.as_bytes())
        }
        "/font.woff2" => respond(&mut stream, 200, "font/woff2", FONT_WOFF2),
        "/api/drives" => match api_drives() {
            Ok(body) => respond_with(&mut stream, 200, json, body.as_bytes(), &extra),
            Err(e) => respond_error(&mut stream, &e),
        },
        "/api/search" => match api_search(query) {
            Ok(body) => respond_with(&mut stream, 200, json, body.as_bytes(), &extra),
            Err(e) => respond_error(&mut stream, &e),
        },
        "/api/list" => match api_list(query) {
            Ok(body) => respond_with(&mut stream, 200, json, body.as_bytes(), &extra),
            Err(e) => respond_error(&mut stream, &e),
        },
        _ => respond(&mut stream, 404, "text/plain; charset=utf-8", b"not found"),
    }
}
```

`serve()`를 고친다.

```rust
pub fn serve(port: u16, open_browser: bool) -> Result<()> {
    // 비밀번호가 없으면 뜨지 않는다. 인증 없이 열리는 경로를 남겨 두면
    // 언젠가 그 상태로 터널이 붙는다. 인덱스에는 파일 이름과 경로가 통째로 있다.
    if !crate::auth::is_configured() {
        // 로그온 작업은 S4U로 돌아 stderr가 아무에게도 보이지 않는다.
        // 왜 죽었는지를 로그에 남겨야 사람이 알 수 있다.
        crate::sync::log("serve: 비밀번호가 설정되어 있지 않아 뜨지 않음");
        anyhow::bail!(
            "웹 화면 비밀번호가 설정되어 있지 않습니다.\n\
             `drive-archive passwd`로 먼저 설정하세요."
        );
    }

    let listener = TcpListener::bind((Ipv4Addr::LOCALHOST, port)).map_err(|e| {
        crate::sync::log(&format!("serve: {port}번 포트를 열지 못함 ({e})"));
        anyhow::anyhow!("{port}번 포트를 열 수 없습니다 (이미 쓰고 있는지 확인하세요): {e}")
    })?;

    let state = Arc::new(ServeState {
        dir: crate::db::data_dir()?,
        sessions: Default::default(),
        gate: Default::default(),
    });

    let url = format!("http://127.0.0.1:{port}/");
    println!("drive-archive 웹 화면: {url}");
    println!("밖에서 보시려면 이 주소로 터널을 붙이세요.");
    println!("끝내려면 Ctrl+C를 누르세요.");

    if open_browser {
        let _ = std::process::Command::new("cmd")
            .args(["/C", "start", "", &url])
            .spawn();
    }

    for stream in listener.incoming() {
        let Ok(stream) = stream else { continue };
        let st = Arc::clone(&state);
        std::thread::spawn(move || {
            if let Err(e) = handle(stream, &st) {
                eprintln!("요청 처리 실패: {e:#}");
            }
        });
    }
    Ok(())
}
```

기존 테스트 `페이지와_폰트를_내려준다`는 두 가지 이유로 고쳐야 한다. `handle`이 상태를
받게 되었고, 인증이 생기면서 **모르는 경로는 404가 아니라 401이 먼저 나온다** — 어떤
경로가 있는지조차 로그인 없이는 알려 주지 않는 것이 맞다. 통째로 갈아낀다.

```rust
    #[test]
    fn 페이지와_폰트를_내려준다() {
        let (_d, st) = 준비된_상태("열려라참깨입니다");
        let st = std::sync::Arc::new(st);
        let listener = TcpListener::bind((Ipv4Addr::LOCALHOST, 0)).unwrap();
        let port = listener.local_addr().unwrap().port();
        {
            let st = std::sync::Arc::clone(&st);
            std::thread::spawn(move || {
                for s in listener.incoming().take(3) {
                    let _ = handle(s.unwrap(), &st);
                }
            });
        }

        let get = |path: &str| -> String {
            let mut s = TcpStream::connect((Ipv4Addr::LOCALHOST, port)).unwrap();
            s.write_all(format!("GET {path} HTTP/1.1\r\nHost: x\r\n\r\n").as_bytes())
                .unwrap();
            read_all(s)
        };

        let page = get("/");
        assert!(page.starts_with("HTTP/1.1 200 OK"), "{}", &page[..60.min(page.len())]);
        assert!(page.contains("DRIVE-ARCHIVE"));

        let font = get("/font.woff2");
        assert!(font.starts_with("HTTP/1.1 200 OK"));
        assert!(font.contains("font/woff2"));

        // 로그인 전에는 모르는 경로도 401이다.
        let missing = get("/없는경로");
        assert!(missing.starts_with("HTTP/1.1 401"));
    }
```

- [ ] **Step 4: 테스트가 통과하는 것을 확인한다**

Run: `cargo test`
Expected: 전부 통과. `cargo test serve::`는 기존 5개(하나는 401 기대로 고침) + Task 5의 10개 + 8개

- [ ] **Step 5: 손으로 확인한다**

```bash
cargo run -- serve --no-open
```

Expected: 비밀번호가 설정되어 있으면 뜬다. `auth.json`을 잠시 다른 이름으로 옮기고 실행하면 안내와 함께 거부한다 (확인 뒤 되돌린다).

```bash
curl -i http://127.0.0.1:8787/api/drives
```

Expected: `HTTP/1.1 401 Unauthorized`

- [ ] **Step 6: 커밋 (승인 후)**

```bash
git add src/serve.rs
git commit -m "모든 요청에 세션을 요구한다"
```

---

### Task 7: 화면 — PASSWORD 게이트

**Files:**
- Modify: `src/web/index.html`

**Interfaces:**
- Consumes: `POST /api/login`, 401 응답 (Task 6)
- Produces: `state.gate` (불리언), `showGate()`, `tryLogin()`, `api(url, opts)` 래퍼

- [ ] **Step 1: CSS를 더한다**

`<style>` 끝에 넣는다.

```css
  /* ── 비밀번호 게이트 ───────────────────────────────── */
  /* 비밀번호를 묻는 동안에는 표와 목록을 감춘다. 자리는 남겨 두지 않는다 —
     빈 표가 보이면 뒤에 뭔가 있다는 인상을 준다. */
  body.gate #rule,
  body.gate #head,
  body.gate #list,
  body.gate #more,
  body.gate #vim,
  body.gate #mode { display: none; }

  /* `#where`는 남긴다. 틀렸다는 것과 잠금이 얼마나 남았는지 적을 자리가 필요하고,
     그 줄에는 인덱스 내용이 들어가지 않는다. */
```

- [ ] **Step 2: 상태와 프롬프트를 고친다**

`const PROMPT = 'SEARCH: ';`를 지우고 함수로 바꾼다.

```js
const SEARCH_PROMPT   = 'SEARCH: ';
const PASSWORD_PROMPT = 'PASSWORD: ';

function prompt_() { return state.gate ? PASSWORD_PROMPT : SEARCH_PROMPT; }

/** 화면에 그릴 입력 글자. 비밀번호는 별표로 가린다. */
function shownValue(s) {
  return state.gate ? '*'.repeat([...s].length) : s;
}
```

`state`에 `gate: false,`를 더한다.

`paintSearch()`를 고친다.

```js
function paintSearch() {
  const value = el.keeper.value;
  el.shown.textContent = prompt_() + shownValue(value);

  // 커서는 입력 지점 위에 놓는다. IME 조합 중에도 selectionStart가 따라온다.
  const at = el.keeper.selectionStart ?? value.length;
  const col = width(prompt_()) + width(shownValue(value.slice(0, at)));
  el.cursor.style.left = `${col}ch`;

  el.cursor.classList.toggle('hidden', !state.insert);
  el.cursor.classList.toggle('blink', state.insert);
}
```

별표는 한 칸씩 세므로 한글 비밀번호에서도 커서가 맞는다. `width()`는 별표를 1칸으로 세고, 문자 수만큼 별표를 찍었기 때문이다.

- [ ] **Step 3: 게이트 진입과 로그인을 더한다**

```js
/* ── 비밀번호 ──────────────────────────────────────── */

/** 게이트로 들어간다. 목록을 지우고 비밀번호를 묻는다. */
function showGate(note) {
  state.gate = true;
  state.rows = [];
  state.hits = [];
  state.drives = [];
  state.browse = null;
  state.origin = null;
  document.body.classList.add('gate');
  el.keeper.value = '';
  el.where.textContent = note || '';
  setInsert(true);
  paintSearch();
}

/** 게이트를 나간다. */
function leaveGate() {
  state.gate = false;
  document.body.classList.remove('gate');
  el.keeper.value = '';
  el.where.textContent = '';
  paintSearch();
  loadDrives();
}

async function tryLogin() {
  const password = el.keeper.value;
  if (!password) return;
  el.where.textContent = '확인하는 중...';
  try {
    const r = await fetch('/api/login', {
      method: 'POST',
      body: JSON.stringify({ password }),
    });
    if (r.ok) { leaveGate(); return; }
    const data = await r.json().catch(() => ({}));
    el.keeper.value = '';
    paintSearch();
    if (r.status === 429) {
      el.where.textContent = `너무 여러 번 틀렸습니다. ${data.seconds || 60}초 뒤에 다시 시도하세요.`;
    } else {
      el.where.textContent = 'WRONG PASSWORD';
    }
  } catch (e) {
    el.where.textContent = '연결할 수 없습니다: ' + e.message;
  }
}
```

- [ ] **Step 4: 401을 한 곳에서 잡는다**

`loadDrives`·`runSearch`·`openDir`이 각각 `fetch`를 부른다. 셋 다 세션이 끊기면 게이트로 돌아가야 하므로 래퍼를 하나 만들고 세 곳이 그것을 쓰게 한다.

```js
/** 인덱스를 읽는 요청. 세션이 끊겼으면 게이트로 돌려보낸다. */
async function api(url) {
  const r = await fetch(url);
  if (r.status === 401) {
    showGate('세션이 끝났습니다. 다시 로그인하세요.');
    throw new Error('로그인이 필요합니다');
  }
  return r.json();
}
```

세 함수에서 `const r = await fetch(...); const data = await r.json();`를 `const data = await api(...);`로 바꾼다.
그리고 세 함수의 `catch` 맨 앞에 `if (state.gate) return;`을 넣는다 — `api()`가 게이트로
돌려보낸 직후에 오류 문구가 게이트 안내("세션이 끝났습니다...")를 덮어쓰면 안 된다.

- [ ] **Step 5: Enter와 입력을 게이트에 맞춘다**

`onInput()` 맨 앞에 넣는다 — 비밀번호를 치는 동안 검색이 나가면 안 된다.

```js
function onInput() {
  paintSearch();
  if (state.gate) return;      // 비밀번호를 치는 중이다. 검색하지 않는다.
  clearTimeout(searchTimer);
  const q = el.keeper.value;
  searchTimer = setTimeout(() => runSearch(q), 140);
}
```

`enter()` 맨 앞에 넣는다.

```js
function enter() {
  if (state.gate) { tryLogin(); return; }
  // ...나머지는 그대로...
}
```

키 핸들러의 ESC 처리에도 게이트를 반영한다. 비밀번호를 치는 중에 VIM NORMAL로 빠지면 입력이 막힌다.

```js
    case 'Escape':
      if (state.gate) return;      // 게이트에서는 모드를 바꾸지 않는다
      e.preventDefault();
      state.vim = true;
      setInsert(false);
      return;
```

- [ ] **Step 6: 시작할 때 세션을 확인한다**

맨 아래 `loadDrives();`를 `boot();`로 바꾸고 함수를 더한다.

```js
/** 세션이 있으면 목록을, 없으면 게이트를 띄운다. */
async function boot() {
  try {
    const r = await fetch('/api/drives');
    if (r.status === 401) { showGate(); return; }
    const data = await r.json();
    if (data.error) throw new Error(data.error);
    state.drives = data.drives;
    state.kind = 'drive';
    state.rows = state.drives;
    state.note = state.drives.length ? '' : '인덱싱된 하드가 없습니다.';
  } catch (e) {
    state.note = '인덱스를 읽을 수 없습니다: ' + e.message;
    state.rows = [];
  }
  paintAll();
}
```

- [ ] **Step 7: 손으로 확인한다**

```bash
cargo run -- serve --no-open
```

브라우저에서 `http://127.0.0.1:8787/`를 연다.

Expected:
- `PASSWORD:`가 뜬다. 표와 목록은 보이지 않는다
- 글자를 치면 `*`가 하나씩 는다. 커서가 별표 끝을 따라간다
- 틀린 비밀번호 + Enter → `WRONG PASSWORD`, 입력칸이 비워진다
- 맞는 비밀번호 + Enter → 하드 목록이 뜨고 `SEARCH:`로 바뀐다
- 새로고침해도 목록이 그대로 (쿠키가 살아 있다)
- 여섯 번 틀리면 남은 초를 알려 준다

- [ ] **Step 8: 커밋 (승인 후)**

```bash
git add src/web/index.html
git commit -m "PASSWORD 게이트 — 통과해야 목록이 보인다"
```

---

### Task 8: 화면 — 적응형

**Files:**
- Modify: `src/web/index.html`

**Interfaces:**
- Produces: `lineH()` — 지금 줄 높이를 px로 잰다. `LINE` 상수를 대체한다.

41칸은 그대로 두고 글자 크기만 화면 폭에 맞춘다. 데스크톱은 18px 상한이라 지금 화면이 그대로다.

- [ ] **Step 1: 고정폭 비율을 실측한다**

CSS를 쓰기 전에 FiraD2의 문자 폭이 글자 크기의 몇 배인지 재야 한다. 추정하지 않는다.

서버를 띄우고 브라우저 콘솔에서 실행한다.

```js
// 폰트가 아직 로드 중이면 대체 폰트로 재게 된다. 로드를 기다렸다가 잰다.
document.fonts.ready.then(() => {
  const s = document.createElement('span');
  s.style.cssText = 'position:absolute;visibility:hidden;font:18px FiraD2';
  s.textContent = 'M'.repeat(100);
  document.body.appendChild(s);
  console.log(s.getBoundingClientRect().width / 100 / 18);   // 이 값이 비율
  s.remove();
});
```

나온 값을 아래 CSS의 `--ratio`에 적는다. Fira 계열은 보통 0.6 언저리이지만, 실제로 나온 값을 쓴다.

- [ ] **Step 2: CSS를 고친다**

`:root`에 더한다.

```css
    /* 고정폭 한 칸의 폭 ÷ 글자 크기. 위 Step 1에서 실측한 값이다.
       41칸이 화면에 정확히 들어가야 해서 추정값을 쓰지 않는다. */
    --ratio: 0.6;
    /* 좌우 여백의 합. body padding과 맞춰 둔다. */
    --pad: calc(min(81px, 6vw) + 24px);
```

`body`의 `font`를 고친다.

```css
  body {
    margin: 0;
    background: var(--blue);
    color: var(--fg);
    /* 데스크톱은 18px 그대로다. 화면이 좁아지면 41칸이 들어가는 크기까지 줄인다.
       칸 수를 줄이지 않는 이유는 표 그리기가 전부 41칸을 전제로 짜여 있어서다. */
    font-size: min(18px, calc((100vw - var(--pad)) / (var(--cols) * var(--ratio))));
    line-height: var(--line);
    font-family: 'FiraD2', 'D2Coding', 'Consolas', monospace;
    padding: 42px 24px 42px min(81px, 6vw);
    display: flex;
    flex-direction: column;
    overflow: hidden;
    -webkit-font-smoothing: antialiased;
  }
```

`--line`을 글자 크기에 비례하게 바꾼다. 지금 값 21px는 18px 글자에 대한 것이므로 비율은 7/6이다.

```css
    --line: 1.1667em;   /* 21px / 18px. 글자 크기가 줄면 줄 높이도 따라 준다 */
```

`pre.deco`와 `.bar`, `.row` 등은 `var(--line)`을 쓰므로 자동으로 따라온다. 다만 `--line`이 `em`이 되었으므로, `:root`가 아니라 `body`에 두어야 body의 글자 크기를 기준으로 계산된다. `:root`에 두면 루트의 16px 기준으로 계산되어 어긋난다.

**`--line` 선언을 `:root`에서 `body`로 옮긴다.**

- [ ] **Step 3: JS의 LINE 상수를 없앤다**

`const LINE = 21;`을 지우고 함수로 바꾼다.

```js
/** 지금 줄 높이(px). 글자 크기가 화면 폭에 따라 달라지므로 상수로 둘 수 없다. */
function lineH() {
  return parseFloat(getComputedStyle(document.body).lineHeight) || 21;
}
```

`fitList()`와 `page()`에서 `LINE`을 `lineH()`로 바꾼다.

```js
function fitList() {
  const line = lineH();
  // ...기존 로직에서 LINE 대신 line을 쓴다...
}

function page() {
  return Math.max(1, Math.round(el.list.clientHeight / lineH()));
}
```

- [ ] **Step 4: 손으로 확인한다**

Run: `cargo run -- serve --no-open`

Expected:
- 데스크톱 창을 넓게 두면 지금과 똑같은 화면이다 (글자 18px)
- 창을 좁히면 글자가 작아지면서 41칸이 계속 한 줄에 들어간다. 가로 스크롤이 생기지 않는다
- 표의 열이 어긋나지 않는다
- 마지막 줄이 반만 보이지 않는다 (`fitList`가 새 줄 높이로 다시 계산한다)

Chrome 개발자 도구의 기기 도구 모음에서 iPhone 세로(390px)로 바꿔 확인한다.

Expected: 41칸이 잘리지 않고 들어간다. 헤더 `HDD`가 하드 이름 첫 글자와 세로로 나란하다.

- [ ] **Step 5: 커밋 (승인 후)**

```bash
git add src/web/index.html
git commit -m "화면 폭에 맞춰 글자 크기를 줄인다 — 41칸은 그대로"
```

---

### Task 9: 화면 — 터치 조작

**Files:**
- Modify: `src/web/index.html`

모바일에서 더블탭은 확대로 먹힌다. 이미 선택된 행을 다시 탭하면 열리게 한다. 데스크톱 더블클릭은 그대로 둔다.

- [ ] **Step 1: 행 클릭 처리를 고친다**

`paintList()` 안의 `div.addEventListener('mousedown', ...)`를 찾아 고친다.

```js
    div.addEventListener('mousedown', e => {
      e.preventDefault();
      // 이미 고른 줄을 다시 누르면 연다. 모바일에서 더블탭은 확대로 먹히기 때문이다.
      // 데스크톱에서도 같은 줄을 두 번 누르면 열리는데, 자연스러운 동작이라 그대로 둔다.
      if (state.sel === i) { enter(); return; }
      select(i);
    });
```

`i`가 그 행의 인덱스여야 한다. 기존 코드에서 인덱스를 어떻게 넘기는지 확인하고 맞춘다.

- [ ] **Step 2: 확대를 막지 않는다**

`<meta name="viewport">`에 `user-scalable=no`를 넣지 않는다. 글자가 작아지는 화면이라 사용자가 확대할 수 있어야 한다. 지금 값을 그대로 둔다.

- [ ] **Step 3: 손으로 확인한다**

Chrome 기기 도구 모음에서 iPhone 세로로 두고 확인한다.

Expected:
- 항목을 한 번 탭하면 선택만 된다. 하단에 그 항목이 어느 하드에 있는지 뜬다
- 같은 항목을 다시 탭하면 열린다 (하드면 최상위, 폴더면 그 안)
- `..`을 탭하면 위로 올라간다
- 검색창을 탭하면 소프트 키보드가 뜬다

- [ ] **Step 4: 커밋 (승인 후)**

```bash
git add src/web/index.html
git commit -m "고른 줄을 다시 누르면 열린다 — 모바일에서 더블탭이 안 먹힌다"
```

---

### Task 10: task.rs — serve 작업 등록

**Files:**
- Modify: `src/task.rs`

**Interfaces:**
- Produces:
  - `pub const SYNC_TASK: &str = "drive-archive sync"` (기존 `TASK_NAME`을 개명)
  - `pub const SERVE_TASK: &str = "drive-archive serve"`
  - `fn serve_task_xml(exe: &Path) -> Result<String>`
  - `pub fn setup(exe: &Path) -> Result<()>` — 두 작업을 모두 등록
  - `pub fn remove() -> Result<bool>` — 두 작업을 모두 제거
  - `pub fn run_serve_now() -> Result<()>` — 등록 직후 웹 화면을 깨운다 (Task 11이 쓴다)
  - 내부 함수 `register(name: &str, xml: &str) -> Result<()>`, `delete(name: &str) -> Result<bool>`, `exists_named(name: &str) -> bool`, `run_named(name: &str) -> Result<()>`

기존 `setup`·`remove`·`exists`·`run_now`가 `TASK_NAME`을 박아 쓰고 있다. 작업 이름을 인자로 받는 형태로 쪼개고, 기존 호출부가 쓰던 `setup`/`remove`는 두 작업을 함께 다루도록 바꾼다. `run_now`는 sync 작업만 깨우면 되므로 `SYNC_TASK`를 그대로 쓴다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```rust
    #[test]
    fn serve_작업은_로그온에_뜨고_시간_제한이_없다() {
        let xml = serve_task_xml(Path::new(r"C:\bin\drive-archive.exe")).unwrap();
        assert!(xml.contains("<LogonTrigger>"));
        // sync의 PT4H를 그대로 쓰면 서버가 4시간 뒤에 죽는다.
        assert!(xml.contains("<ExecutionTimeLimit>PT0S</ExecutionTimeLimit>"), "{xml}");
        assert!(!xml.contains("PT4H"));
    }

    #[test]
    fn serve_작업은_마운트_이벤트를_듣지_않는다() {
        // 하드를 꽂았다고 웹 서버를 다시 띄울 이유가 없다.
        let xml = serve_task_xml(Path::new(r"C:\bin\drive-archive.exe")).unwrap();
        assert!(!xml.contains("EventTrigger"));
    }

    #[test]
    fn serve_작업은_창_없이_실행된다() {
        let xml = serve_task_xml(Path::new(r"C:\bin\drive-archive.exe")).unwrap();
        assert!(xml.contains("<LogonType>S4U</LogonType>"));
        assert!(xml.contains("<Hidden>true</Hidden>"));
    }

    #[test]
    fn serve_작업은_브라우저를_열지_않는다() {
        // 로그온할 때마다 브라우저가 뜨면 안 된다.
        let xml = serve_task_xml(Path::new(r"C:\bin\drive-archive.exe")).unwrap();
        assert!(xml.contains("<Arguments>serve --no-open</Arguments>"), "{xml}");
    }

    #[test]
    fn 두_작업의_이름이_다르다() {
        assert_ne!(SYNC_TASK, SERVE_TASK);
        let sync = task_xml(Path::new(r"C:\bin\drive-archive.exe")).unwrap();
        let serve = serve_task_xml(Path::new(r"C:\bin\drive-archive.exe")).unwrap();
        assert!(sync.contains(&format!("<URI>\\{SYNC_TASK}</URI>")));
        assert!(serve.contains(&format!("<URI>\\{SERVE_TASK}</URI>")));
    }

    #[test]
    fn serve_작업도_한_번만_돈다() {
        // 두 개가 뜨면 뒤엣것이 포트를 못 잡는다.
        let xml = serve_task_xml(Path::new(r"C:\bin\drive-archive.exe")).unwrap();
        assert!(xml.contains("<MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>"));
    }
```

- [ ] **Step 2: 테스트가 실패하는 것을 확인한다**

Run: `cargo test task::`
Expected: 컴파일 실패 — `serve_task_xml`, `SYNC_TASK`, `SERVE_TASK`가 없다

- [ ] **Step 3: 구현한다**

`TASK_NAME`을 `SYNC_TASK`로 개명하고(참조하는 곳을 모두 고친다) 상수를 하나 더한다.

```rust
pub const SYNC_TASK: &str = "drive-archive sync";
pub const SERVE_TASK: &str = "drive-archive serve";
```

`serve_task_xml`을 더한다. sync의 XML과 다른 곳은 트리거·인자·시간 제한 세 곳이다.

```rust
/// 웹 화면을 로그온할 때 띄우는 작업.
///
/// sync 작업과 세 곳이 다르다. 트리거는 로그온뿐이고(하드를 꽂았다고 서버를 다시
/// 띄울 이유가 없다), 인자는 `serve --no-open`이며(로그온마다 브라우저가 뜨면 안
/// 된다), `ExecutionTimeLimit`이 `PT0S`다 — sync의 `PT4H`를 그대로 쓰면 서버가
/// 네 시간 뒤에 죽는다.
fn serve_task_xml(exe: &Path) -> Result<String> {
    let user = std::env::var("USERNAME").context("USERNAME 환경 변수를 읽을 수 없습니다")?;
    let domain = std::env::var("USERDOMAIN").unwrap_or_else(|_| ".".to_string());
    let exe = exe.display().to_string();

    Ok(format!(
        r#"<?xml version="1.0" encoding="UTF-16"?>
<Task version="1.4" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <RegistrationInfo>
    <Description>drive-archive 웹 화면을 띄웁니다. 컴퓨터가 켜져 있는 동안에만 접속할 수 있습니다.</Description>
    <URI>\{SERVE_TASK}</URI>
  </RegistrationInfo>
  <Triggers>
    <LogonTrigger>
      <Enabled>true</Enabled>
      <Delay>PT30S</Delay>
    </LogonTrigger>
  </Triggers>
  <Principals>
    <Principal id="Author">
      <UserId>{domain}\{user}</UserId>
      <LogonType>S4U</LogonType>
      <RunLevel>LeastPrivilege</RunLevel>
    </Principal>
  </Principals>
  <Settings>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <DisallowStartIfOnBatteries>false</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>false</StopIfGoingOnBatteries>
    <AllowHardTerminate>true</AllowHardTerminate>
    <StartWhenAvailable>false</StartWhenAvailable>
    <RunOnlyIfNetworkAvailable>false</RunOnlyIfNetworkAvailable>
    <IdleSettings>
      <StopOnIdleEnd>false</StopOnIdleEnd>
      <RestartOnIdle>false</RestartOnIdle>
    </IdleSettings>
    <AllowStartOnDemand>true</AllowStartOnDemand>
    <Enabled>true</Enabled>
    <Hidden>true</Hidden>
    <RunOnlyIfIdle>false</RunOnlyIfIdle>
    <WakeToRun>false</WakeToRun>
    <ExecutionTimeLimit>PT0S</ExecutionTimeLimit>
    <Priority>7</Priority>
  </Settings>
  <Actions Context="Author">
    <Exec>
      <Command>{exe}</Command>
      <Arguments>serve --no-open</Arguments>
    </Exec>
  </Actions>
</Task>
"#
    ))
}
```

등록·삭제·확인을 이름으로 받게 쪼갠다.

```rust
/// 작업 하나를 등록한다. 이미 있으면 새 정의로 덮어쓴다.
fn register(name: &str, xml: &str) -> Result<()> {
    let tmp = std::env::temp_dir().join(format!("{}.xml", name.replace(' ', "-")));
    std::fs::write(&tmp, to_utf16le_bom(xml))
        .with_context(|| format!("작업 정의를 쓸 수 없습니다: {}", tmp.display()))?;

    let out = Command::new("schtasks")
        .args(["/Create", "/TN", name, "/XML"])
        .arg(&tmp)
        .arg("/F")
        .output()
        .context("schtasks를 실행할 수 없습니다")?;

    let _ = std::fs::remove_file(&tmp);

    if !out.status.success() {
        bail!(
            "{name} 등록에 실패했습니다:\n{}",
            decode_console(&out.stderr, &out.stdout)
        );
    }
    Ok(())
}

fn delete(name: &str) -> Result<bool> {
    if !exists_named(name) {
        return Ok(false);
    }
    let out = Command::new("schtasks")
        .args(["/Delete", "/TN", name, "/F"])
        .output()
        .context("schtasks를 실행할 수 없습니다")?;
    if !out.status.success() {
        bail!(
            "{name} 삭제에 실패했습니다:\n{}",
            decode_console(&out.stderr, &out.stdout)
        );
    }
    Ok(true)
}

fn exists_named(name: &str) -> bool {
    Command::new("schtasks")
        .args(["/Query", "/TN", name])
        .output()
        .map(|o| o.status.success())
        .unwrap_or(false)
}

pub fn exists() -> bool {
    exists_named(SYNC_TASK)
}
```

`setup`과 `remove`가 두 작업을 다루게 한다.

```rust
/// 자동 인덱싱과 웹 화면 작업을 함께 등록한다.
pub fn setup(exe: &Path) -> Result<()> {
    if !crate::elevation::is_elevated() {
        bail!(
            "작업 스케줄러에 등록하려면 관리자 권한이 필요합니다.\n\
             PowerShell을 마우스 오른쪽 버튼으로 눌러 '관리자 권한으로 실행'한 뒤 다시 시도하세요."
        );
    }
    register(SYNC_TASK, &task_xml(exe)?)?;
    register(SERVE_TASK, &serve_task_xml(exe)?)?;
    Ok(())
}

/// 등록한 작업을 모두 제거한다. 하나라도 지웠으면 참이다.
pub fn remove() -> Result<bool> {
    if !exists_named(SYNC_TASK) && !exists_named(SERVE_TASK) {
        return Ok(false);
    }
    if !crate::elevation::is_elevated() {
        bail!(
            "작업 스케줄러 등록을 해제하려면 관리자 권한이 필요합니다.\n\
             PowerShell을 '관리자 권한으로 실행'한 뒤 다시 시도하세요."
        );
    }
    let a = delete(SYNC_TASK)?;
    let b = delete(SERVE_TASK)?;
    Ok(a || b)
}
```

`run_now`를 이름을 받는 꼴로 쪼개고, serve 쪽도 깨울 수 있게 한다. 기존 `run_now`의
본문과 주석은 `run_named`로 옮긴다.

```rust
fn run_named(name: &str) -> Result<()> {
    let out = Command::new("schtasks")
        .args(["/Run", "/TN", name])
        .output()
        .context("schtasks를 실행할 수 없습니다")?;
    if !out.status.success() {
        bail!("{name} 작업을 시작하지 못했습니다:\n{}", decode_console(&out.stderr, &out.stdout));
    }
    Ok(())
}

pub fn run_now() -> Result<()> {
    run_named(SYNC_TASK)
}

/// 등록 직후 웹 화면을 바로 띄울 때 쓴다. 다음 로그온을 기다리게 하지 않는다.
pub fn run_serve_now() -> Result<()> {
    run_named(SERVE_TASK)
}
```

- [ ] **Step 4: 테스트가 통과하는 것을 확인한다**

Run: `cargo test task::`
Expected: 기존 8개 + 6개 통과

- [ ] **Step 5: 커밋 (승인 후)**

```bash
git add src/task.rs
git commit -m "웹 화면을 로그온에 띄우는 작업을 더한다"
```

---

### Task 11: install.rs — 비밀번호 강제

**Files:**
- Modify: `src/install.rs`

**Interfaces:**
- Consumes: `crate::prompt_and_set_password()` (Task 4), `auth::is_configured()` (Task 1), `task::setup/remove/run_serve_now` (Task 10)

`install`은 비밀번호를 받아야 끝난다. 설치를 마친 상태에는 비밀번호가 반드시 있고, 그래서 `serve`가 인증 없이 열릴 수 있는 경우가 사라진다.

- [ ] **Step 1: install()을 고친다**

`task::setup(&exe)?;` 앞에 비밀번호 단계를 넣고, 단계 번호를 `[1/3]`·`[2/3]`·`[3/3]`으로 고친다.

```rust
    println!("drive-archive 설치를 시작합니다.");
    println!("  실행 파일: {}\n", exe.display());

    // 웹 화면은 밖에서도 들어올 수 있다. 비밀번호 없이 설치를 끝내면 그 상태로
    // 터널이 붙는다. 그래서 여기서 반드시 받는다.
    if auth::is_configured() {
        println!("[1/3] 웹 화면 비밀번호는 이미 설정되어 있습니다.");
        println!("      바꾸시려면 나중에 `drive-archive passwd`를 실행하세요.");
    } else {
        println!("[1/3] 웹 화면에서 쓸 비밀번호를 정해 주세요.");
        println!("      인덱스에는 파일 이름과 경로가 모두 들어 있습니다. 밖에서 들어오려면");
        println!("      이 비밀번호가 필요합니다. 입력하는 글자는 화면에 찍히지 않습니다.\n");
        loop {
            match crate::prompt_and_set_password() {
                Ok(()) => break,
                Err(e) => {
                    eprintln!("      {e}");
                    eprintln!("      다시 입력해 주세요.\n");
                }
            }
        }
        println!("      비밀번호를 저장했습니다.");
    }

    task::setup(&exe)?;
    println!("\n[2/3] 자동 인덱싱과 웹 화면을 등록했습니다.");
    println!("      외장하드를 연결하면 인덱스가 자동으로 갱신됩니다.");
    println!("      웹 화면은 로그인할 때 자동으로 뜹니다: http://127.0.0.1:8787/");

    // 등록만 하고 끝내면 다음 로그온까지 웹 화면이 없다. v0.2.1에서 sync를
    // 등록 직후 깨운 것과 같은 이유로, 여기서 한 번 띄운다.
    match task::run_serve_now() {
        Ok(()) => println!("      웹 화면을 지금 띄우도록 요청했습니다."),
        Err(e) => eprintln!("      웹 화면을 바로 띄우지는 못했습니다 ({e:#}). 다음 로그온부터 뜹니다."),
    }
```

MCP 단계를 `[3/3]`으로 바꾸고, 마지막 안내에 터널과 비밀번호 변경을 더한다.

```rust
    println!("\n설치가 끝났습니다. 외장하드를 연결해 보세요.");
    println!("비밀번호를 바꾸시려면 `drive-archive passwd`를 실행하세요.");
    println!("밖에서 보시려면 http://127.0.0.1:8787/ 에 터널을 붙이세요 (README 참고).");
    println!("주의: 이 실행 파일을 다른 폴더로 옮기면 `install`을 다시 실행해야 합니다.");
```

`uninstall()`의 문구도 두 작업을 지운다는 뜻으로 고친다.

```rust
    if task::remove()? {
        println!("[1/2] 자동 인덱싱과 웹 화면 등록을 해제했습니다.");
    } else {
        println!("[1/2] 등록된 작업이 없었습니다.");
    }
```

`uninstall`은 `auth.json`을 지우지 않는다. 인덱스를 남기는 것과 같은 이유다 — 다시 설치하면 그대로 쓴다. 마지막에 한 줄로 알린다.

```rust
    println!("\n인덱스와 웹 비밀번호는 남아 있습니다.");
    println!("완전히 지우시려면 %LOCALAPPDATA%\\drive-archive 폴더를 삭제하세요.");
```

`use` 목록에 `crate::auth`를 더한다.

- [ ] **Step 2: 빌드가 되는지 확인한다**

Run: `cargo build`
Expected: 경고 없이 성공

- [ ] **Step 3: 테스트가 여전히 통과하는지 확인한다**

Run: `cargo test`
Expected: 전부 통과

- [ ] **Step 4: 손으로 확인한다**

관리자 PowerShell에서 실행한다. 개발 세션의 셸은 비관리자 토큰이라 등록이 거부된다.

Run: `cargo run -- install`

Expected:
- 비밀번호가 없으면 물어본다. 짧게 넣으면 되묻는다
- 이미 있으면 묻지 않고 넘어간다
- `schtasks /Query /TN "drive-archive serve"`로 작업이 보인다

- [ ] **Step 5: 커밋 (승인 후)**

```bash
git add src/install.rs
git commit -m "설치할 때 비밀번호를 반드시 받는다"
```

---

### Task 12: 실기 검증과 문서

**Files:**
- Modify: `README.md`, `README_en.md`, `STATUS.md`, `CLAUDE.md`, `Cargo.toml` (버전)

- [ ] **Step 1: 릴리스로 빌드한다**

MCP 서버가 exe를 붙잡고 있으면 덮어쓸 수 없다. 먼저 정리한다.

```powershell
Get-Process drive-archive -ErrorAction SilentlyContinue | Stop-Process -Force
cargo build --release
```

- [ ] **Step 2: 로그온 작업이 실제로 뜨는지 확인한다**

**이것이 이번 작업에서 가장 불확실한 지점이다.** S4U는 자격 증명을 저장하지 않아 네트워크 리소스 접근이 제한된다. 인바운드 리스닝은 로컬 자원이라 될 것으로 보이지만 확인되지 않았다.

```powershell
schtasks /Run /TN "drive-archive serve"
Start-Sleep -Seconds 3
Get-Process drive-archive | Select-Object Id, MainWindowHandle
Test-NetConnection -ComputerName 127.0.0.1 -Port 8787
```

Expected: 프로세스가 살아 있고 `MainWindowHandle`이 0이며(창이 없다) 포트가 열려 있다.

**안 되면:** `serve_task_xml`의 `LogonType`을 `InteractiveToken`으로 내린다. 콘솔 창은 코드 쪽 숨김 메커니즘(v0.2.2)이 막는다. 바꿨으면 `install`을 다시 실행해야 반영된다 — 실행 파일만 다시 빌드하는 것으로는 옛 정의가 남는다.

- [ ] **Step 3: 터널을 붙여 밖에서 들어와 본다**

`cloudflared`를 설치하고 quick tunnel을 띄운다.

```powershell
cloudflared tunnel --url http://127.0.0.1:8787
```

화면에 뜨는 `https://<임의>.trycloudflare.com` 주소로 휴대폰에서 접속한다.

Expected:
- `PASSWORD:`가 뜬다
- 맞는 비밀번호로 목록이 보인다
- 브라우저 개발자 도구에서 쿠키에 `Secure`가 붙어 있다 (터널이 `X-Forwarded-Proto: https`를 보냈다)
- 41칸이 잘리지 않는다
- 탭으로 선택, 재탭으로 열기가 된다

- [ ] **Step 4: 검증용 비밀번호를 바꾸도록 안내한다**

개발 중에 쓴 임시 비밀번호가 실사용 비밀번호로 남으면 안 된다. 황보정님께 `drive-archive passwd`로 직접 바꾸시라고 알린다. 이 단계는 사람이 해야 하므로 대신 하지 않는다.

- [ ] **Step 5: 숫자를 실측한다**

문서에 적을 값을 재서 넣는다. 추측하지 않는다.

```powershell
cargo test 2>&1 | Select-String "test result"          # 테스트 수
(Get-ChildItem src/*.rs | Get-Content | Measure-Object -Line).Lines   # 코드 줄 수
(Get-Item target/release/drive-archive.exe).Length     # 실행 파일 크기
cargo tree -e normal --prefix none | Sort-Object -Unique | Measure-Object   # 크레이트 수
```

- [ ] **Step 6: 문서를 고친다**

`Cargo.toml`의 버전을 `0.4.0`으로 올린다.

**README.md**에 더할 것:
- `passwd` 명령
- 밖에서 보는 방법: Cloudflare Tunnel(도메인이 있으면 고정 주소) 또는 Tailscale Funnel(도메인 없이 고정 주소, 포트 443/8443/10000)
- 컴퓨터가 꺼져 있으면 접속되지 않는다는 것
- 로그온하면 웹 화면이 자동으로 뜬다는 것

**README_en.md**도 같은 내용으로 맞춘다.

**STATUS.md**:
- 맨 위 최종 갱신·버전·상태
- 기능 표에 "웹 비밀번호", "적응형 화면", "웹 화면 자동 실행" 행 추가
- 규모 항목의 숫자를 Step 5에서 잰 값으로 교체
- 개발 이력에 `### v0.4.0 — 밖에서 보는 인덱스 (2026-08-31)` 절 추가. 무엇을 왜 그렇게 했는지, 검증한 방법, 만들면서 고친 결함을 적는다
- 기술 결정 기록에 "argon2는 크레이트를 썼다"와 "인증에 예외를 두지 않는다"를 남긴다
- 인덱싱된 하드 표의 숫자도 지금 값으로 맞춘다 (Works A가 빠져 있다)

**CLAUDE.md**의 "알아둘 것"에 웹 화면 비밀번호를 한 줄 더한다.

- [ ] **Step 7: 전체 테스트를 돌린다**

Run: `cargo test`
Expected: 전부 통과

- [ ] **Step 8: 커밋 (승인 후)**

```bash
git add -A
git commit -m "v0.4.0: 밖에서도 인덱스를 본다"
```

---

## 자체 점검

**spec 대응:**

| spec 요구사항 | 태스크 |
|---|---|
| 1. 외부 접속 | Task 6(서버), Task 12(터널 검증) |
| 2. `PASSWORD:` 게이트, SEARCH 폼 재사용 | Task 7 |
| 3. `*`로 표시 | Task 7 Step 2 |
| 4. 게이트에서 리스트뷰 숨김 | Task 7 Step 1 |
| 5. 통과하면 목록 | Task 7 Step 3 |
| 6. 적응형 | Task 8, Task 9 |
| 7. 컴퓨터가 켜져 있을 때만 | Task 10, Task 11 |
| 8. 터미널에서만 비밀번호 | Task 4 |
| 9. install이 비밀번호를 강제 | Task 11 |
| 10. LAN에 직접 열지 않음 | Task 6 (`Ipv4Addr::LOCALHOST` 유지, `--host` 없음) |
| 비밀번호 없으면 serve 거부 | Task 6 Step 3 |
| 세션 24시간 슬라이딩 | Task 2, Task 6 |
| 무차별 대입 잠금(주소별+전역) | Task 3, Task 6 |
| `Secure`는 https일 때만 | Task 6 |

**이름 일관성:** `set_password_at`/`verify_at`/`is_configured_at`(Task 1) → Task 6의 `verify_at`, Task 11의 `is_configured`가 같은 이름을 쓴다. `Sessions::check`(Task 2) → Task 6의 `authed`가 부른다. `Gate::locked_for`/`note_failure`/`note_success`(Task 3) → Task 6의 `api_login`이 부른다. `prompt_and_set_password`(Task 4) → Task 11이 부른다. `SYNC_TASK`/`SERVE_TASK`/`run_serve_now`(Task 10) → Task 11의 안내 문구와 즉시 실행. `Request`/`read_request`(Task 5) → Task 6의 `handle`.

**남은 불확실성:** Task 12 Step 2의 S4U 리스닝. 대안(`InteractiveToken`)을 그 자리에 적어 두었다.
