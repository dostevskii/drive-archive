# 프로젝트 현황

**최종 갱신:** 2026-08-31 · **버전:** v0.4.0 · **상태:** 구현·자동 검증 완료, 실기 검증 일부 대기 (아래 남은 일)

---

## 지금 상태

기능이 모두 구현되었고, 자동 검증(테스트 138개)과 로컬 실기 검증(실제 Chrome)을 마쳤습니다. 외부에서 접속하는 경로의 실기 검증 — 관리자 `install`, 로그온 작업 기동, 터널·휴대폰 접속 — 은 [남은 일](#남은-일)에 목록으로 있습니다. FAT32 볼륨의 마운트 이벤트는 종전대로 확인하지 못했습니다(이 컴퓨터에 FAT32 하드가 없음).

| 항목 | 상태 |
|---|---|
| 인덱싱 (파일·폴더 메타데이터) | 동작 |
| 변경 감지 (추가/변경/삭제 diff) | 동작 |
| 검색 (한글·확장자·경로) | 동작 |
| MCP 서버 (Claude 연동) | 동작 · Claude Code 연결 확인 |
| 인덱스 보호 안전장치 | 동작 · 스캔 강제 중단으로 확인 |
| 모든 파일 시스템 지원 | 동작 · exFAT 실기 확인 |
| 자동 인덱싱 (작업 스케줄러) | 동작 · 등록·실행 확인 |
| `install` 원커맨드 설치 | 동작 · UAC 상승 경로 확인 |
| 마운트 이벤트 트리거 발화 | 동작 · 하드 재연결로 확인 |
| 콘솔 창 숨김 | 동작 · 대조군 비교 · S4U 등록 확인 |
| 꽂은 하드만 스캔 | 동작 · 값 쿼리 치환 실기 확인 |
| 브라우저 웹 화면 (`serve`) | 동작 · 실제 Chrome에서 검색·탐색·선택·VIM 확인 |
| 웹 비밀번호 게이트 | 동작 · 단위 테스트 + 실제 Chrome 확인 |
| 적응형 화면 (모바일) | 동작 · 390px 실측 확인 |
| 웹 화면 자동 실행 등록 | 동작 · install 등록·S4U 기동·포트 응답 확인 |
| PATH 자동 등록 | 동작 · 새 터미널에서 bare 명령 확인 |

### 인덱싱된 하드 (2026-08-31 기준)

| 라벨 | 포맷 | 항목 수 | 볼륨 시리얼 |
|---|---|---|---|
| Works A | exFAT | 87,803 | A83D09DF |
| Works C | NTFS | 49,570 | 44C550BC |
| Works D | NTFS | 22,662 | 90FA8BC5 |
| Works E | NTFS | 171,139 | 0480390F |
| Works F | NTFS | 0 (빈 하드) | C482BDB6 |

전체 331,174개 항목 · 인덱스 108MB · `%LOCALAPPDATA%\drive-archive\index.db`

**Works C는 2026-08-30에 자동으로 들어왔습니다.** 처음 보는 4TB 하드를 꽂았더니 사람이 손대지 않은 채 15:06:36에 스캔이 시작되어 88초 만에 49,570개가 등록되었습니다. 마운트 이벤트 트리거가 새 하드를 잡아 처음부터 인덱싱하는 경로가 실사용에서 확인된 셈입니다.

**Works A도 같은 날 자동으로 들어왔습니다.** v0.3.1 문서를 쓰는 동안(2026-08-30 15:23) 마운트 트리거가 4TB exFAT 하드를 잡아 87,803개를 인덱싱했고, 그래서 당시 문서의 하드 표에는 빠져 있었습니다. 위 표가 그것을 바로잡은 것입니다.

2026-08-28에 Works F의 자료를 Works E로 옮기고 Works F를 NTFS로 다시 포맷했습니다. 포맷으로 볼륨 시리얼이 `6C965A22`에서 `C482BDB6`으로 바뀌어, 옛 Works F 항목은 가리킬 대상이 없는 상태가 되었습니다. 인덱스를 통째로 비우고 세 하드를 처음부터 다시 훑었습니다.

### 규모

Rust 4,995줄 (테스트 포함) · 11개 모듈 · 테스트 138개 · 웹 화면 HTML 813줄 · 실행 파일 3.61MB

의존성은 직접 9개, 릴리스 빌드에 실제로 들어가는 크레이트가 60→72개입니다(`cargo tree -e normal` 기준). v0.4.0에서 비밀번호 해싱에 argon2 크레이트 12개를 더했습니다.

---

## 남은 일

황보정님이 수행할 실기 검증 항목입니다.

- 상시 터널은 Tailscale Funnel로 구축했습니다 (2026-08-31). 기기 이름을 중립적으로 바꿔 데스크톱 이름 노출을 없앴고, 공개 DNS·인증서 발급·인그레스 경유 접속(HTTP 200)까지 확인했습니다. **주소는 공개 저장소인 이 문서에 적지 않습니다.** 새 주소로 휴대폰 접속·로그인·목록까지 실기 확인 완료 (2026-08-31)
- 참고: 서버 컴퓨터 자신에서는 Funnel 주소가 열리지 않습니다 (Tailscale의 자기 접속 미지원 — 로컬에서는 127.0.0.1:8787 사용)
- Secure 쿠키 실물 확인 (선택) — 다른 기기 브라우저 개발자 도구에서. 헤더 경로 자체는 단위 테스트로 고정되어 있음
- 터미널에서 passwd 대화형 에코 억제 확인
- ~~아이폰 별표 높이가 글자 수마다 널뜀 (2026-08-31)~~ — Fira Code 계열의 문맥 조합(calt)이 연속 별표를 재배치하는 것이 원인. 별표 칸에서 liga·calt를 꺼서 개수와 무관하게 같은 글리프가 되도록 수정, 3·10개 실측으로 균일 확인
- ~~아이폰 목록이 한 줄만 보임 (2026-08-31)~~ — fitList()가 게이트(목록 display:none) 상태에서 높이를 0으로 재고 Math.max(1,0)=1줄로 굳던 결함. 게이트에서는 재지 않고 leaveGate에서 다시 재도록 수정
- ~~아이폰 실사용 보고 2건 (2026-08-31)~~ — ① 비밀번호 별표가 줄 위로 떠 보임: '*' 글리프가 위첨자 높이인 글꼴 특성. 흐름은 그대로 두고 그리기만 0.18em 내려 수정, 데스크톱 스크린샷으로 정렬 확인. ② 목록을 탭할 때마다 가상 키보드가 올라옴: 아무 탭이나 입력칸 포커스를 되돌리던 데스크톱용 코드가 원인. 터치 기기에서는 되돌림을 끄고 목록 탭은 포커스를 떼도록 수정 — 키보드는 검색줄 탭에서만 뜸
- ~~잠금(429) 화면 문구 확인~~ — 아이폰 실기에서 6회 오답 후 남은 초 안내 노출 확인 (2026-08-31). 문구가 한 줄 말줄임에 잘리던 것은 게이트 한정 줄바꿈으로 수정
- ~~휴대폰 첫 진입 시 화면이 확대되어 보인 관찰 1회~~ — `text-size-adjust: 100%` 적용 후 아이폰 12 미니 재접속에서 정상 확인 (2026-08-31)
- ~~아이폰에서 비밀번호 칸 포커스 시 살짝 확대되던 문제~~ — iOS의 16px 미만 입력칸 자동 확대. 터치 기기 한정 16px 고정으로 수정, 아이폰 12 미니 재관찰에서 해결 확인 (2026-08-31)
- 검증용 임시 비밀번호(개발 중 사용)를 실사용 값으로 교체했는지 확인 (`drive-archive passwd`)
- 비밀번호 미설정 시 serve 거부 확인 — auth.json을 잠시 다른 이름으로 옮기고 `drive-archive serve` 실행, 안내와 함께 거부되고 sync.log에 사유가 남는지 확인 후 되돌리기
- `uninstall` 후 이미 떠 있던 웹 화면이 내려가는지 확인 — 안 내려가면 로그오프 안내가 필요

**작업 정의를 바꾸면 재등록해야 적용됩니다.** `task.rs`를 고쳤다면 관리자 권한으로 `drive-archive install`을 한 번 실행해야 스케줄러에 반영됩니다. 실행 파일만 다시 빌드하는 것으로는 옛 정의가 그대로 남습니다.

### 다음 단계 후보 (아직 착수 안 함)

- **USN 저널 증분 스캔** — 하드 하나를 꽂으면 그 하드만 훑도록 v0.2.2에서 좁혔지만, 그 하드는 여전히 전체를 다시 읽습니다. 다만 급하지 않습니다. 같은 171,139개 항목을 exFAT에서는 약 10분에 훑었는데 NTFS에서는 **8초에서 116초 사이**였습니다(2026-08-28·29 실측). 편차는 Windows 파일 캐시 상태에서 옵니다 — 하드를 막 다시 꽂은 콜드 상태가 116초, 연이어 훑어 캐시가 완전히 더워진 상태가 8초입니다. 실사용에 가까운 값은 콜드 쪽입니다. 느렸던 원인은 항목 수가 아니라 exFAT였습니다. USN 저널을 읽으면 바뀐 것만 반영할 수 있지만 관리자 권한이 필요하고 NTFS에서만 되므로, 지금 성능으로는 값어치가 크지 않습니다.
- **I/O 우선순위 낮추기** — 지금은 CPU 우선순위(`BELOW_NORMAL_PRIORITY_CLASS`)만 낮춥니다. 대용량 복사와 스캔이 겹치면 HDD에서 속도 저하가 있을 수 있습니다(실측 안 함). `PROCESS_MODE_BACKGROUND_BEGIN`을 쓰면 I/O도 낮아지지만 스캔 자체가 크게 느려지는 맞교환이 있습니다. 당장은 복사 같은 큰 작업 전에 `schtasks /Change /TN "drive-archive sync" /DISABLE`로 꺼 두는 것으로 충분합니다 (관리자 권한 없이 됩니다).
- **FAT32 마운트 이벤트 확인** — exFAT에서 Ntfs 이벤트 98이 발생하는 것은 확인했으나, FAT32 볼륨이 없어 확인하지 못했습니다. 로그온 트리거가 안전망 역할을 합니다.

---

## 개발 이력

### v0.4.0 — 외부에서도 인덱스 확인 (2026-08-31)

`serve`에 비밀번호 게이트를 더해, 터널을 경유한 외부 접속도 보호합니다.

**무엇을 했는지:** 모든 HTTP 요청에 유효한 세션(24시간, 활동하는 동안 계속 연장)을 요구합니다. 예외는 게이트 화면·폰트·로그인 엔드포인트뿐입니다. 비밀번호가 설정되어 있지 않으면 serve가 뜨지 않고, `install`이 비밀번호를 반드시 받습니다. 비밀번호 설정·변경은 터미널 전용(passwd 명령, 에코 없음). 화면은 41칸을 유지한 채 글자 크기만 화면 폭에 맞춥니다(FiraD2 폭 비율 0.6154 실측). 선택된 줄을 재탭으로 열 수 있습니다. 로그온할 때마다 자동으로 serve를 띄우는 작업(S4U, ExecutionTimeLimit PT0S)을 등록합니다.

**보안 장치:** argon2id 해시(auth.json, 인덱스 DB와 분리), 무차별 대입 잠금 — 주소별 5회/60초 + 전역 20회(리셋 창 600초), X-Forwarded-For 위조 대비. HTTP 계층 상한 — 읽기 타임아웃 10초, 요청 64KB, 헤더 16KB, 본문 8KB. Secure 쿠키는 X-Forwarded-Proto: https일 때만. 전역 잠금에는 맞교환이 있습니다 — 터널 주소를 아는 사람이 분당 한 번 실패 요청을 보내면 새 로그인이 계속 막힙니다(이미 로그인된 세션은 영향 없음). 단일 사용자 도구라 수용한 잔여 위험입니다.

**만들면서 고친 결함 4가지:**

1. 전역 잠금 카운터의 리셋 창이 잠금 시간(60초)과 같아, "20회 몰아치기 → 61초 대기 → 반복"으로 우회 가능했습니다. 리셋 창을 600초로 분리하고, 기록이 남아 있는 동안은 실패 한 번에 재잠금되도록 고쳤습니다. 그래야 어떤 속도로 두드리든 분당 한 번 수준으로만 진행됩니다.

2. /api/login의 500 응답이 anyhow 오류 전문을 그대로 내보내, auth.json의 전체 경로가 익명 호출자에게 노출될 수 있었습니다. 상세는 stderr로만 기록하고 밖으로는 일반 문구만 보냅니다.

3. 선택된 `..`를 더블클릭하면 동기 화면 전환 후, 같은 클릭의 dblclick이 새로 그려진 다른 줄에서 재발화해 엉뚱한 하드가 열렸습니다. dblclick 리스너를 제거했습니다(두 번째 클릭이 이미 열기).

4. --line 주석에 틀린 근거(CSS 커스텀 속성 em이 선언 요소 기준이라는 주장)가 들어갔습니다. 원문 주석으로 정정했습니다. 미등록 커스텀 속성의 상대 단위는 사용 지점 기준으로 풀립니다.

**검증:** 단위 테스트 90→138 (48개 추가). 실제 Chrome에서 게이트·오답·세션 유지·검색, 1280px/390px 적응형, curl로 미인증 401 확인.

**설치 경로도 실기로 확인했습니다 (2026-08-31).** 관리자 `install`이 네 단계를 모두 밟았고 — 비밀번호는 이미 있어 건너뜀, 두 작업 등록, PATH 등록, Store판 Claude Desktop과 Claude Code 양쪽 MCP 연결 — 새 터미널에서 `drive-archive --help`가 경로 없이 동작했습니다. **S4U 우려는 기우였습니다.** 등록 직후 깨운 serve 작업이 `상태: Running`으로 떠 있고, 프로세스의 `MainWindowHandle`이 0(창 없음)이며, 127.0.0.1:8787이 TCP로 응답하고 `/api/drives`가 401, `/`가 200을 돌려줬습니다. S4U 로그온 작업에서 TCP 리스닝이 된다는 것이 실사용으로 증명되어 `InteractiveToken` 대안은 쓸 일이 없어졌습니다. 터널·휴대폰 접속은 실기 검증 대기입니다 (남은 일 참조).

**CLI 안내를 영문으로 바꿨습니다.** 셸에 닿는 출력 전부 — 프롬프트, 단계 안내, 오류, `--help` — 가 대상입니다. 웹 화면 문구·코드 주석·작업 스케줄러 설명은 한국어로 남습니다. 사용자 지시에 따른 변경입니다.

**`install`이 사용자 PATH(`HKCU\Environment`)에도 실행 파일 폴더를 등록합니다.** 새 터미널부터는 전체 경로 없이 `drive-archive`만 쳐도 실행됩니다. `setx`나 PowerShell `[Environment]::SetEnvironmentVariable`을 쓰지 않은 이유는 둘 다 `Path` 값을 항상 `REG_SZ`로 다시 쓰기 때문입니다 — 기존 값이 `REG_EXPAND_SZ`였다면(즉 `%SystemRoot%\...`처럼 展開이 필요한 항목이 하나라도 있었다면) 그 항목들이 展開되지 않는 문자 그대로의 문자열로 굳어버립니다. 그래서 레지스트리를 직접 읽고 써서 원래 타입을 그대로 유지합니다(`envpath.rs`). 등록은 새로 여는 터미널부터 적용되며, 이미 열려 있던 터미널에는 반영되지 않습니다. `uninstall`은 등록한 항목을 다시 제거합니다.

### v0.3.1 — Store판 Claude Desktop 대응 (2026-08-30)

**Claude Desktop에 MCP가 등록되지 않는 결함을 고쳤습니다.** 사용자가 데스크톱 앱에서 물었더니 "drive-archive라는 이름의 MCP는 연결된 목록에 없습니다"라는 답이 돌아와 드러났습니다.

원인은 배포판에 따라 설정 파일 위치가 다르다는 것이었습니다. Claude Desktop을 **Microsoft Store(MSIX)판**으로 설치하면 앱이 샌드박스 안에서 돌기 때문에, `%APPDATA%`에 쓴다고 여기는 내용이 패키지 폴더로 리디렉션됩니다.

| 배포판 | 설정 파일 실제 위치 |
|---|---|
| 웹에서 받은 설치판 | `%APPDATA%\Claude\claude_desktop_config.json` |
| Store(MSIX)판 | `%LOCALAPPDATA%\Packages\Claude_<게시자>\LocalCache\Roaming\Claude\claude_desktop_config.json` |

`install.rs`는 `%APPDATA%` 한 곳만 봤고, Store판만 깔린 이 컴퓨터에는 그 폴더가 아예 없습니다. `register_mcp`는 부모 폴더가 없으면 `Ok(false)`로 건너뛰므로, **실패한 것이 아니라 시도조차 되지 않았습니다.** 화면에는 "Claude를 찾지 못해 MCP 연결은 건너뛰었습니다"라고 찍혔을 텐데, Claude Code 쪽 등록은 성공해서 그 문구조차 나오지 않았습니다.

`claude_desktop_configs()`가 두 위치를 모두 찾도록 고쳤습니다. 패키지 폴더 이름에는 게시자 해시가 붙으므로(`Claude_pzs8sxrjxfjjc`) 이름을 박아 두지 않고 `%LOCALAPPDATA%\Packages`에서 `Claude_`로 시작하는 폴더를 훑되, 설정 폴더가 실제로 있는 것만 남깁니다. 두 판을 함께 설치했다면 양쪽에 등록합니다.

**검증:** 실기로 확인했습니다. 빌드한 exe로 `install`을 실행하니 Store판 설정에 `mcpServers.drive-archive`가 들어갔고, 기존 최상위 키 3개와 `preferences` 27개 항목이 하나도 바뀌지 않은 것을 백업본과 대조해 확인했습니다. 웹 설치판 경로는 없으므로 종전대로 건너뜁니다. 단위 테스트 3개를 더해 90개가 되었습니다(Store판 경로 탐색, Claude 아닌 패키지 제외, `Packages` 폴더 부재).

**놓쳤던 이유:** 설정 파일이 하나뿐이라고 가정하고 경로를 상수로 박아 두었습니다. Claude Code 쪽이 성공하는 바람에 "적어도 한 곳에는 등록됨"으로 보여 결함이 가려졌습니다. 등록되지 않은 대상을 이름으로 알려 주지 않는 지금 구조에서는 같은 일이 또 가려질 수 있습니다.

### v0.3.0 — 브라우저로 인덱스 보기 (2026-08-29)

Claude를 거치지 않고 사용자가 직접 인덱스를 들여다볼 수 있는 화면을 더했습니다. `drive-archive serve`를 실행하면 127.0.0.1에 웹 화면이 뜹니다. 디자인은 Figma 시안(Drive-Archive-02)을 따라 파란 바탕에 41칸 고정폭 TUI 형태이고, 폰트는 [FiraD2](https://github.com/partrita/FiraD2) Regular입니다.

**웹 서버도 직접 구현했습니다.** MCP 때와 같은 판단입니다 — 필요한 것이 로컬호스트에서 GET 네 개(화면·폰트·하드 목록·검색)에 답하는 것뿐이라 `std::net`으로 충분했고, 크레이트 수가 하나도 늘지 않았습니다. 화면(HTML/CSS/JS)과 폰트는 `include_str!`·`include_bytes!`로 실행 파일에 넣어, 배포할 파일이 여전히 exe 하나입니다.

**127.0.0.1에만 바인딩합니다.** 인덱스에는 사용자의 파일 이름과 경로가 통째로 들어 있어서, 같은 네트워크의 다른 기기에 열어 줄 이유가 없습니다.

화면에서 되는 것:

- 검색 전 기본 화면은 하드 목록입니다 (라벨 · 볼륨 시리얼 · 포맷 · 폴더 수). 연결되지 않은 하드는 흐리게 표시합니다.
- 검색창은 영문과 한글을 받습니다. 커서는 터미널처럼 블록으로 깜빡입니다.
- 선택 이동은 마우스 클릭, 화살표 키, VIM 키(`hjkl`) 세 가지입니다. `ESC`로 VIM NORMAL 모드에 들어가고 `i` 또는 `/`로 입력으로 돌아옵니다. 하단 `VIM MODE` 글자를 눌러도 켜고 끌 수 있습니다.
- 선택한 항목이 **어느 하드에 있는지**를 하단에 항상 띄웁니다. 이 도구의 결론에 해당하는 줄이라 41칸 제한을 두지 않았습니다.
- 검색 결과의 하드 이름 칸은 표의 오른쪽 끝에 붙습니다. 칸 폭을 실제 이름 길이에 맞춰 잡으므로, 헤더 `HDD`가 `Works D`의 `W`와 세로로 나란해집니다.
- 목록 아래에 더 있으면 `-- MORE --`가 뜹니다. 끝까지 내리면 사라집니다.
- **하드를 열어 폴더를 따라 들어갈 수 있습니다.** 하드를 고르고 `Enter`를 치면 그 하드의 최상위가 펼쳐지고, 폴더에서 다시 `Enter`를 치면 그 안으로 들어갑니다. 맨 위 `..` 줄로 한 단계 올라가고, 루트에서 더 올라가면 하드 목록으로 돌아옵니다. 더블클릭으로도 열립니다. 하드가 꽂혀 있지 않아도 인덱스에 남은 내용을 그대로 훑어볼 수 있습니다.

**한글은 두 칸으로 셉니다.** 고정폭 폰트에서도 한글은 영문의 두 배 폭이라, 커서 위치와 이름 잘라내기를 칸 수로 계산합니다. `SEARCH: ` 다음에 한글 세 자를 치면 커서가 정확히 14칸 자리에 섭니다.

**검증:** 실제 Chrome에서 확인했습니다. 영문(`Portfolio`)·한글(`리허설`) 검색이 모두 결과를 냈고, 화살표·`j`/`k`·마우스 클릭으로 선택이 옮겨지며 하단 경로가 따라 바뀌는 것을 확인했습니다. `ESC`로 `--NORMAL--`, 클릭으로 `VIM MODE [ON]`이 되는 것도 확인했습니다. 릴리스 빌드로 다시 띄워 같은 화면이 나오는 것까지 봤습니다. 테스트는 84개로 늘었습니다(웹 서버 5개 추가).

**만들면서 고친 결함 네 가지:**

1. `SEARCH:` 글자가 화면에서 사라졌습니다. 검색 줄에 `white-space: pre`가 걸려 있어 HTML 태그 사이의 줄바꿈과 들여쓰기가 그대로 글자로 찍혔고, 그만큼 밀려난 텍스트가 `overflow: hidden`에 잘렸습니다. 그 줄은 한 줄로 붙여 써야 합니다(주석으로 남겨 두었습니다).
2. 하드 목록 응답이 검색 결과를 덮어썼습니다. 페이지를 열자마자 검색어를 치면, 뒤늦게 도착한 목록이 방금 띄운 결과를 지웠습니다. 검색어가 남아 있으면 기본 화면으로 되돌리지 않도록 고쳤습니다.
3. `percent_decode`가 패닉할 수 있었습니다. `%` 뒤 두 글자를 문자열로 잘라 읽었는데, 인코딩되지 않은 한글이 뒤따르면(`%한`) 글자 중간에서 잘려 터집니다. 바이트로 읽도록 바꾸고 테스트를 더했습니다.
4. 검색 상한에 걸린 결과를 정확한 건수처럼 보여 줬습니다. 200건에서 끊긴 것을 `200 FOUND`라고 적으면 거짓말이 됩니다. 서버가 한 건 더 조회해 상한 도달을 알리고, 화면은 `200+ FOUND`로 적습니다.
5. **검색을 하면 위쪽 두 막대가 눌려 올라갔습니다.** 사용자가 발견해 알려 준 결함입니다. `body`가 flex 컨테이너인데 `.bar`에 `flex: none`이 없어, 목록이 길어지면 브라우저가 남는 공간을 만들려고 막대를 세로로 눌렀습니다. 실측하니 높이가 21px에서 **4px**로 찌그러졌고, 그래서 회색 막대보다 글자가 커져 파란 바탕으로 삐져나온 것처럼 보였습니다. 마스크를 씌운 것이 아니라 flex 축소였습니다. 줄어들면 안 되는 요소에 `flex: none`을 붙여 고쳤습니다.

**화면을 쓰면서 사용자가 짚어 준 것 세 가지를 더 고쳤습니다.**

- **줄이 반만 보였습니다.** 목록 칸 높이가 줄 높이(21px)의 배수가 아니어서 마지막 줄이 잘렸습니다. `fitList()`가 목록 높이를 21의 배수로 고정합니다. 남는 몇 픽셀은 바탕으로 둡니다.
- **`-- MORE --`가 줄 전체를 덮었습니다.** 글자에만 반전이 걸리도록 안쪽 `span`으로 옮겼습니다.
- **`ESC`를 눌러도 검색창이 키를 붙들고 있었습니다.** NORMAL로 바꾸면서 입력칸의 포커스를 떼지 않아, 한글 IME가 그 칸을 계속 잡고 있었습니다. 그 상태에서 `j`를 치면 `ㅓ` 조합으로 들어가고, 키 처리기가 첫 줄의 `isComposing`에서 멈춰 hjkl이 아예 동작하지 않았습니다. NORMAL로 갈 때 `blur()`를 부르도록 고쳤습니다. 확인해 보니 `ESC` 뒤 포커스가 `BODY`로 넘어가고, `j`·`k`·`l`이 선택을 옮기며 검색어는 그대로 남습니다.
- **검색 결과에서 연 폴더에서 나오면 결과가 사라졌습니다.** `..`가 언제나 상위 폴더로만 올라가서, `Portfolio\리허설`을 열었다가 나오면 검색 결과가 아니라 `Portfolio`가 나왔습니다. 검색 결과에서 들어온 자리(하드·경로·선택 위치)를 적어 두었다가, 올라오는 길에 그 자리에 닿으면 결과로 되돌립니다. 결과는 들고 있던 것을 그대로 쓰므로 다시 조회하지 않고, 선택했던 줄과 `200+` 표시까지 복원됩니다. 더 깊이 들어갔다 나오는 경우에도 진입 지점에서 빠져나옵니다. 하드 목록에서 연 폴더는 종전대로 하드 목록으로 돌아갑니다.
- **폴더를 따라 들어갈 수 없었습니다.** `list_children()`과 `/api/list`를 더해 인덱스를 파일 브라우저처럼 훑을 수 있게 했습니다. 경로를 `LIKE`로 맞추지 않고 글자 수로 앞부분을 잘라 비교합니다. 폴더 이름에 `_`가 들어가면(`A_B`) `LIKE`는 `AXB`의 파일까지 끌어오기 때문입니다. 그 경우를 덮는 테스트를 넣었습니다.

**README 스크린샷은 예시 데이터로 찍었습니다.** 처음 찍은 화면에는 실제 작업 파일 이름이 그대로 들어가 있었습니다. 공개 저장소에 올라가면 클라이언트 이름과 작업 이력이 검색 가능해지므로, `LOCALAPPDATA`를 옮겨 만든 별도 인덱스에 예시 데이터를 넣고 다시 찍었습니다.

**아직 확인하지 않은 것:** `--no-open` 없이 실행했을 때 기본 브라우저가 자동으로 열리는지는 확인하지 않았습니다(검증하는 동안 창이 계속 뜨는 것을 피하려고 껐습니다). 실패해도 서버는 그대로 뜨고 주소가 화면에 찍힙니다.

### v0.2.2 — 콘솔 창 숨김, 꽂은 하드만 스캔 (2026-08-29)

사용자를 방해하던 두 가지를 없앴습니다.

**콘솔 창.** 스케줄러가 깨울 때마다 대화형 세션에 검은 콘솔 창이 떠서, 아무것도 하지 않았는데 화면에 창이 나타났습니다. 두 겹으로 막았습니다. 작업 정의의 `LogonType`을 `InteractiveToken`에서 `S4U`로 바꿔 대화형 세션에 아예 붙지 않게 했고, 코드에서도 자기 콘솔 창을 숨깁니다. 코드 쪽은 콘솔에 붙은 프로세스가 자기 하나뿐일 때만 숨기므로, 사용자가 터미널에서 직접 실행하면 출력이 그대로 보입니다.

살아 있는 프로세스 둘을 나란히 놓고 확인했습니다. `cmd.exe`는 `MainWindowHandle`이 2949986이었고, `drive-archive sync`는 0이었습니다.

**전체 스캔.** 하드 하나를 꽂아도 연결된 하드를 전부 다시 훑었습니다. Works D를 꽂았는데 Works E의 171,139개를 116초 동안 헛되이 읽는 식이었습니다. 마운트 이벤트에는 어느 볼륨이 붙었는지가 `DriveName`(`L:` 형태)으로 담겨 있어서, 작업 스케줄러의 값 쿼리로 그 값을 꺼내 `sync --drive`로 넘깁니다.

값이 없는 경로도 함께 덮었습니다. 로그온 트리거나 `schtasks /Run`으로 실행되면 `$(DriveName)`이 치환되지 않은 채 넘어오는데, 드라이브 문자로 읽히지 않는 값은 무시하고 전부 확인합니다. 내장 디스크에서 이벤트가 나면(`C:` 등) 인덱싱 대상이 아니므로 그냥 물러납니다 — 이전에는 그때도 외장하드를 전부 훑었습니다.

**검증:** 재등록한 뒤 하드를 뽑았다 꽂아 확인했습니다. 등록된 정의에 `S4U`와 값 쿼리가 들어갔고, 그 실행이 남긴 로그는 꽂은 하드 한 줄이 전부였습니다.

```
[12:49:17] Works F (I:) 스캔 시작                  ← 재등록 전: 세 하드 전부
[12:49:17] Works E (J:) 스캔 시작
[12:49:23] Works D (L:) 스캔 시작

[12:51:03] Works D (L): 방금 스캔했으므로 건너뜀    ← 재등록 후: 이 한 줄뿐
```

Works E와 F는 언급조차 되지 않았습니다. 스케줄러가 이벤트에서 `L:`을 꺼내 넘긴 것이 맞습니다. 그 실행은 쿨다운(120초)에 걸려 스캔까지 가지 않았지만, 확인하려던 것은 어느 하드를 대상으로 삼는가였고 그것이 증명되었습니다.

### 마운트 이벤트 트리거 발화 확인 (2026-08-29)

마지막까지 남아 있던 미검증 항목입니다. 하드를 실제로 뽑았다 꽂아 확인했습니다. 시각이 그대로 이어집니다.

| 시각 | 사건 |
|---|---|
| 12:23:52 | System 로그에 `Microsoft-Windows-Ntfs` 이벤트 98 기록 |
| 12:24:08 | 작업 스케줄러가 `drive-archive sync` 실행 |

두 시각의 차이 16초는 작업 정의에 걸어 둔 `<Delay>PT15S</Delay>`와 맞아떨어집니다. 이번에는 `schtasks /Run`을 부르지 않았으므로, 하드를 꽂은 것만으로 작업이 깨어난 것이 맞습니다. **이벤트 발생 → 트리거 발화 → 스캔 실행**의 전 경로가 확인되었고, 상주 프로세스 없이 하드 연결을 잡아낸다는 설계 전제가 성립합니다.

같은 실행에서 diff도 함께 검증되었습니다. Works E는 171,139개가 그대로라고 정확히 판정했고, Works D에서는 지난 스캔 이후 늘어난 파일 하나를 잡아냈습니다(22,661 → 22,662).

**성능:** Works E의 171,139개를 훑는 데 **116초**가 걸렸습니다. 복사 직후 측정한 41초보다 오래 걸렸는데, 그때는 Windows 파일 캐시가 따뜻했고 이번에는 하드를 막 다시 꽂은 콜드 상태였기 때문으로 보입니다. 실사용에 가까운 값은 116초 쪽입니다.

### v0.2.1 — 등록 직후 바로 인덱싱, 검색 안내 정확도 (2026-08-28)

설치를 끝냈는데도 **이미 꽂혀 있던 하드가 인덱싱되지 않는** 결함을 고쳤습니다. `install`은 작업을 등록만 하고 끝났고, 트리거는 마운트 이벤트와 로그온뿐이라, 등록 시점에 이미 연결되어 있던 하드는 그 이벤트를 놓친 뒤였습니다. 다음 로그온까지 아무 일도 일어나지 않았고, 사용자가 하드를 뽑았다 다시 꽂아야 했습니다.

`task::run_now()`를 추가해 `install`과 `setup-task`가 등록 직후 `schtasks /Run`으로 작업을 한 번 깨웁니다. 실행에 실패해도 등록은 이미 끝났으므로 설치 자체는 성공으로 둡니다.

직접 `sync_all()`을 부르지 않은 이유는 세 가지입니다. `install`은 UAC로 상승된 별도 콘솔에서 이어지므로 거기서 스캔하면 그 창이 끝날 때까지 붙잡힙니다. 작업은 `LeastPrivilege`로 등록되어 있어 `schtasks /Run`을 타면 평소 자동 실행과 같은 일반 권한 경로로 돕니다. 중복 실행은 `MultipleInstancesPolicy=IgnoreNew`와 `InstanceLock`이 이미 막고 있습니다.

**검색 안내도 함께 고쳤습니다.** 검색 결과 맨 아래가 하드의 연결 여부를 보지 않고 무조건 "→ Works D 하드를 연결하세요"라고 안내했습니다. 이미 꽂혀 있는 하드를 서랍에서 헛되이 찾게 만드는 안내였습니다. `split_by_connection`을 두어 꺼내 와야 하는 하드와 이미 연결된 하드를 나눠 알리고, 연결된 쪽에는 드라이브 문자를 덧붙입니다.

판별은 라벨이 아니라 **볼륨 시리얼**로 합니다. 하드를 포맷하면 라벨은 그대로여도 시리얼이 바뀌므로, 라벨로 맞추면 인덱스에 남은 옛 항목을 새 볼륨과 같은 하드로 착각합니다. 아래 Works F 사례가 정확히 그 경우였습니다. `list_drives`가 쓰던 판별 방식을 그대로 따랐습니다.

**검증:** `schtasks /Run`이 비관리자 셸에서도 동작하고 작업이 실제로 깨어나 스캔을 시작하는 것을 실기로 확인했습니다. 빌드한 exe에 새 경로가 들어갔는지는 바이너리 문자열로 대조했습니다. 검색 안내는 연결된 하드로 실제 검색해 확인했고, 연결 여부·포맷된 하드·중복 결과를 덮는 단위 테스트 4개를 더했습니다. 테스트 75개 통과.

**함께 확인한 것:** 스캔 도중 프로세스를 강제 종료해도 인덱스가 온전히 남는 것을 실제로 확인했습니다. `sync_volume`이 스캔을 끝까지 마친 뒤에만 `apply_scan`을 호출하기 때문입니다.

### v0.2.0 — 모든 파일 시스템 지원 (2026-08-26)

파일 시스템 제한을 없앴습니다. 지원 목록을 늘리는 대신 **검사 자체를 제거**해서, Windows가 드라이브 문자를 붙여 준 볼륨이면 무엇이든 인덱싱합니다. HFS+처럼 별도 드라이버가 필요한 형식도 드라이버만 설치하면 코드 변경 없이 잡힙니다.

하드별 포맷을 인덱스에 기록하고 `drives`·`list_drives`에 표시합니다. macOS가 만드는 폴더(`.Spotlight-V100`, `.Trashes` 등)를 제외 목록에 추가했습니다.

**성과:** 그동안 필터에 걸려 통째로 빠져 있던 exFAT 하드(3.6TB)에서 171,139개 항목을 새로 인덱싱했습니다. 기존 NTFS 인덱스 22,661개는 마이그레이션에서 그대로 살아남았습니다.

**바로잡은 것:** "Ntfs 이벤트 98은 NTFS 전용이라 exFAT은 트리거가 안 될 것"이라고 판단해 주석에 적었으나, 이벤트 로그를 확인하니 exFAT 볼륨에서도 발생하고 있었습니다. 추측이 틀려 실측대로 고쳤습니다.

### v0.1.1 — 인덱스 보호 안전장치 (2026-08-26)

스캔 도중 하드가 분리되면 읽다 만 결과가 반영되어 멀쩡한 인덱스가 통째로 지워지는 결함이 있었습니다. 없는 드라이브를 스캔하면 `entries=0, errors=1`로 **정상 반환**되고, 그 결과가 diff에 들어가면 전량 삭제로 이어졌습니다.

세 겹으로 막았습니다.

1. 드라이브 루트를 읽지 못하면 빈 결과가 아니라 실패를 반환 (`scan.rs`)
2. 스캔 후 같은 하드인지 볼륨 시리얼로 재확인 (`volume.rs`의 `still_connected`)
3. 읽기 오류가 있으면서 항목이 이전의 절반 아래로 줄면 반영 보류 (`sync.rs`의 `is_scan_trustworthy`)

오류 없이 끝난 스캔은 결과가 얼마나 줄었든 그대로 믿습니다. 사용자가 정말 하드를 비웠다면 인덱스도 비워지는 것이 맞기 때문입니다.

`SyncOutcome`을 enum으로 바꿔 실패가 화면에 드러나게 했습니다. 이전에는 `sync_all`이 실패를 로그에만 남기고 삼켰습니다.

### v0.1.0 — 첫 릴리스 (2026-08-26)

인덱싱·검색·이벤트 트리거·MCP 서버·원커맨드 설치를 모두 구현했습니다.

**실기 검증:** 1.8TB 하드의 22,661개 항목을 4.8초에 인덱싱. 재스캔 시 diff가 "변경 없음"으로 정확히 판정. Claude Code MCP 클라이언트 연결 성공.

---

## 기술 결정 기록

같은 판단을 다시 하지 않도록, 왜 그렇게 했는지 남깁니다.

### MCP를 직접 구현했습니다 (rmcp 안 씀)

`rmcp` 크레이트를 실제로 넣어 보니 의존성이 81개에서 **127개로 늘고 tokio 런타임이 딸려 왔습니다.** MCP stdio는 줄 단위 JSON-RPC 2.0이 전부이고 읽기 전용 도구가 셋뿐이라, 이미 있던 `serde_json`만으로 약 200줄에 구현했습니다. 프로토콜 정확성은 실제 Claude Code 연결로 검증했습니다.

"굉장히 작은 리소스로 동작"이 이 프로그램의 핵심 요구사항이라, 비동기 런타임을 요청-응답 루프 하나 때문에 넣는 것은 맞지 않다고 판단했습니다.

### 웹 화면도 프레임워크 없이 만들었습니다

`serve`는 `std::net::TcpListener` 위에 직접 올렸습니다. 로컬호스트에서 GET 네 개에 답하는 것이 전부여서, 프레임워크를 넣으면 비동기 런타임까지 딸려 옵니다. MCP를 직접 구현한 것과 같은 이유입니다.

keep-alive를 쓰지 않고 응답마다 연결을 닫습니다. 로컬에서는 다시 여는 비용이 없는 것이나 마찬가지이고, 서버 쪽이 훨씬 단순해집니다. 요청마다 스레드를 하나 띄우므로 브라우저가 화면·폰트·API를 한꺼번에 요청해도 서로 기다리지 않습니다.

### 상주 프로세스를 두지 않습니다

`WM_DEVICECHANGE`를 받으려면 프로세스가 항상 떠 있어야 합니다. 대신 Windows 작업 스케줄러의 **이벤트 트리거**를 씁니다. 평소 메모리·CPU 사용량이 0이고, 하드가 연결될 때만 잠깐 실행되고 끝나면 완전히 종료됩니다. 대가는 연결 후 스캔 시작까지 약 15초 지연입니다.

v0.4.0의 웹 화면은 예외입니다. 로그온 시 서버를 켜 두는 작업을 등록합니다 — '컴퓨터가 켜져 있을 때만 접속된다'는 요구가 상주를 전제하기 때문입니다. 인덱싱 쪽은 종전대로 상주하지 않습니다.

### 하드는 볼륨 시리얼로 식별합니다

드라이브 문자(`E:`, `F:`)는 연결 순서에 따라 바뀝니다. 인덱스에는 볼륨 시리얼만 저장하고, 경로도 드라이브 루트 기준 상대 경로로 넣습니다. 라벨은 사람이 물리 라벨과 대조하는 이름으로만 씁니다.

### 검색은 `LIKE`, FTS 아님

한글 부분 문자열 검색에는 `LIKE '%키워드%'`가 가장 정확합니다. 형태소 분석이 없는 FTS는 한글을 제대로 쪼개지 못합니다. 193,800행에서 체감 지연이 없어 v1에는 충분합니다.

### 파일 내용은 저장하지 않습니다

이름·경로·크기·수정 시각만 기록합니다. 인덱스가 가볍게 유지되고(331,174개 항목에 108MB), 파일 내용이 어딘가로 복사되는 일이 없습니다. 대신 파일 안의 내용은 검색할 수 없습니다.

### 암호 해시는 argon2 크레이트를 썼습니다

MCP·웹서버는 직접 구현했지만 암호 해시는 예외입니다. 직접 구현한 PBKDF2도 테스트 벡터로 검증 가능했지만, 부채널 저항까지 걸려 있어 검증된 구현에 맡겼습니다. 크레이트 12개가 추가되어 60→72개가 되었습니다. 난수는 BCryptGenRandom을 써서 크레이트를 추가하지 않았습니다.

### 인증에 예외를 두지 않습니다

로컬 접속도 비밀번호를 요구합니다. cloudflared 같은 터널은 127.0.0.1로 서버에 붙기 때문에, 서버 입장에서 외부와 로컬을 구별할 수 없습니다. "로컬이면 통과"를 두면 터널 뒤로 뚫립니다. 같은 이유로 `--host` 옵션도 만들지 않았습니다.

---

## 확인된 환경 제약

- **개발 세션의 셸은 비관리자 토큰으로 실행됩니다.** 계정은 Administrators 소속이지만 UAC로 걸러져(`Group used for deny only`, Medium 무결성) 작업 스케줄러 **등록**이 거부됩니다. 단순한 `/SC ONLOGON` 작업 생성조차 `Access is denied`입니다.
- **다만 이미 등록된 작업을 다루는 것은 비관리자로도 됩니다.** `schtasks /Run`, `/End`, `/Change /ENABLE|/DISABLE` 모두 관리자 권한 없이 동작합니다(2026-08-28 확인). 작업 소유자가 본인이기 때문입니다. `run_now`가 성립하는 근거이자, 큰 작업 전에 자동 인덱싱을 잠시 꺼 둘 수 있는 근거입니다.
- **`target/release/drive-archive.exe`는 MCP 서버가 붙잡고 있으면 빌드로 덮어쓸 수 없습니다.** Claude 세션마다 `drive-archive.exe mcp` 프로세스가 하나씩 떠 있어, 여러 세션이 열려 있으면 `cargo build --release`가 `os error 5`로 실패합니다. 빌드 전에 `Get-Process drive-archive | Stop-Process -Force`로 정리하면 됩니다. 다음 요청 때 Claude가 알아서 다시 띄웁니다.
- **이 컴퓨터의 Claude Desktop은 Microsoft Store(MSIX)판입니다.** (`Claude_1.40609.0.0_x64__pzs8sxrjxfjjc`) 설정 파일은 `%APPDATA%\Claude`가 아니라 `%LOCALAPPDATA%\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\`에 있습니다. 앱에서 쓰는 Filesystem·Desktop Commander 같은 도구는 `mcpServers`가 아니라 확장(`Claude Extensions`)으로 설치된 것이라 이 파일과 무관합니다.
- **이 컴퓨터에는 HFS/APFS 드라이버가 없습니다.** Windows 기본 드라이버는 NTFS·exFAT·FAT32·ReFS·UDF뿐입니다. Mac 포맷 하드는 드라이브 문자조차 뜨지 않습니다.
- **빌드 도구:** Rust 1.95 (MSVC), Visual Studio 2019 Build Tools (MSVC 14.29).

---

## 이 문서 관리 규칙

기능을 추가하거나 결함을 고치거나 릴리스할 때 이 문서를 함께 갱신합니다.

- **최종 갱신·버전·상태**를 맨 위에서 고칩니다
- 숫자(항목 수, 테스트 수, 코드 줄 수)는 **추측하지 말고 실제로 측정한 값**을 넣습니다
- 미검증 항목을 검증했으면 [남은 일](#남은-일)에서 지우고 결과를 [개발 이력](#개발-이력)에 적습니다
- 판단이 갈렸던 선택은 [기술 결정 기록](#기술-결정-기록)에 왜 그렇게 했는지 남깁니다
