# 프로젝트 현황

**최종 갱신:** 2026-08-29 · **버전:** v0.2.2 · **상태:** 전 기능 실기 검증 완료

---

## 지금 상태

기능이 모두 구현되어 실기로 검증했습니다. **아래 표에 미검증으로 남은 항목은 없습니다.** 확인하지 못한 것은 FAT32 볼륨에서의 마운트 이벤트 하나뿐인데, 이 컴퓨터에 FAT32 하드가 없어서이며 [다음 단계 후보](#다음-단계-후보-아직-착수-안-함)에 적어 두었습니다.

| 항목 | 상태 |
|---|---|
| 인덱싱 (파일·폴더 메타데이터) | 동작 |
| 변경 감지 (추가/변경/삭제 diff) | 동작 |
| 검색 (한글·확장자·경로) | 동작 |
| MCP 서버 (Claude 연동) | 동작 · Claude Code 연결 확인 |
| 인덱스 보호 안전장치 | 동작 · 스캔 강제 중단으로 확인 |
| 모든 파일 시스템 지원 | 동작 · exFAT 실기 확인 |
| 자동 인덱싱 (작업 스케줄러) | 동작 · 등록·실행 확인 |
| `install` 원커맨드 설치 | 동작 · UAC 상승 경로 확인 |
| 마운트 이벤트 트리거 발화 | 동작 · 하드 재연결로 확인 |
| 콘솔 창 숨김 | 동작 · 대조군 비교 · S4U 등록 확인 |
| 꽂은 하드만 스캔 | 동작 · 값 쿼리 치환 실기 확인 |

### 인덱싱된 하드 (2026-08-29 기준)

| 라벨 | 포맷 | 항목 수 | 볼륨 시리얼 |
|---|---|---|---|
| Works D | NTFS | 22,662 | 90FA8BC5 |
| Works E | NTFS | 171,139 | 0480390F |
| Works F | NTFS | 0 (빈 하드) | C482BDB6 |

전체 193,801개 항목 · 인덱스 63.3MB · `%LOCALAPPDATA%\drive-archive\index.db`

2026-08-28에 Works F의 자료를 Works E로 옮기고 Works F를 NTFS로 다시 포맷했습니다. 포맷으로 볼륨 시리얼이 `6C965A22`에서 `C482BDB6`으로 바뀌어, 옛 Works F 항목은 가리킬 대상이 없는 상태가 되었습니다. 인덱스를 통째로 비우고 세 하드를 처음부터 다시 훑었습니다.

### 규모

Rust 3,222줄 (테스트 포함) · 9개 모듈 · 테스트 79개 · 실행 파일 2.91MB

의존성은 직접 8개, 릴리스 빌드에 실제로 들어가는 크레이트가 60개입니다(`cargo tree -e normal` 기준, dev 포함 시 71개). 이전 문서에 적혀 있던 "전체 81개"는 어느 기준으로도 재현되지 않아 2026-08-28에 실측값으로 바로잡았습니다.

---

## 남은 일

검증해야 할 것은 남아 있지 않습니다. 아래는 하면 좋을 후보이고, 어느 것도 지금 막혀 있지 않습니다.

**작업 정의를 바꾸면 재등록해야 적용됩니다.** `task.rs`를 고쳤다면 관리자 권한으로 `drive-archive install`을 한 번 실행해야 스케줄러에 반영됩니다. 실행 파일만 다시 빌드하는 것으로는 옛 정의가 그대로 남습니다.

### 다음 단계 후보 (아직 착수 안 함)

- **USN 저널 증분 스캔** — 하드 하나를 꽂으면 그 하드만 훑도록 v0.2.2에서 좁혔지만, 그 하드는 여전히 전체를 다시 읽습니다. 다만 급하지 않습니다. 같은 171,139개 항목을 exFAT에서는 약 10분에 훑었는데 NTFS에서는 **8초에서 116초 사이**였습니다(2026-08-28·29 실측). 편차는 Windows 파일 캐시 상태에서 옵니다 — 하드를 막 다시 꽂은 콜드 상태가 116초, 연이어 훑어 캐시가 완전히 더워진 상태가 8초입니다. 실사용에 가까운 값은 콜드 쪽입니다. 느렸던 원인은 항목 수가 아니라 exFAT였습니다. USN 저널을 읽으면 바뀐 것만 반영할 수 있지만 관리자 권한이 필요하고 NTFS에서만 되므로, 지금 성능으로는 값어치가 크지 않습니다.
- **I/O 우선순위 낮추기** — 지금은 CPU 우선순위(`BELOW_NORMAL_PRIORITY_CLASS`)만 낮춥니다. 대용량 복사와 스캔이 겹치면 HDD에서 속도 저하가 있을 수 있습니다(실측 안 함). `PROCESS_MODE_BACKGROUND_BEGIN`을 쓰면 I/O도 낮아지지만 스캔 자체가 크게 느려지는 맞교환이 있습니다. 당장은 복사 같은 큰 작업 전에 `schtasks /Change /TN "drive-archive sync" /DISABLE`로 꺼 두는 것으로 충분합니다 (관리자 권한 없이 됩니다).
- **FAT32 마운트 이벤트 확인** — exFAT에서 Ntfs 이벤트 98이 발생하는 것은 확인했으나, FAT32 볼륨이 없어 확인하지 못했습니다. 로그온 트리거가 안전망 역할을 합니다.

---

## 개발 이력

### v0.2.2 — 콘솔 창 숨김, 꽂은 하드만 스캔 (2026-08-29)

사용자를 방해하던 두 가지를 없앴습니다.

**콘솔 창.** 스케줄러가 깨울 때마다 대화형 세션에 검은 콘솔 창이 떠서, 아무것도 하지 않았는데 화면에 창이 나타났습니다. 두 겹으로 막았습니다. 작업 정의의 `LogonType`을 `InteractiveToken`에서 `S4U`로 바꿔 대화형 세션에 아예 붙지 않게 했고, 코드에서도 자기 콘솔 창을 숨깁니다. 코드 쪽은 콘솔에 붙은 프로세스가 자기 하나뿐일 때만 숨기므로, 사용자가 터미널에서 직접 실행하면 출력이 그대로 보입니다.

살아 있는 프로세스 둘을 나란히 놓고 확인했습니다. `cmd.exe`는 `MainWindowHandle`이 2949986이었고, `drive-archive sync`는 0이었습니다.

**전체 스캔.** 하드 하나를 꽂아도 연결된 하드를 전부 다시 훑었습니다. Works D를 꽂았는데 Works E의 171,139개를 116초 동안 헛되이 읽는 식이었습니다. 마운트 이벤트에는 어느 볼륨이 붙었는지가 `DriveName`(`L:` 형태)으로 담겨 있어서, 작업 스케줄러의 값 쿼리로 그 값을 꺼내 `sync --drive`로 넘깁니다.

값이 없는 경로도 함께 덮었습니다. 로그온 트리거나 `schtasks /Run`으로 실행되면 `$(DriveName)`이 치환되지 않은 채 넘어오는데, 드라이브 문자로 읽히지 않는 값은 무시하고 전부 확인합니다. 내장 디스크에서 이벤트가 나면(`C:` 등) 인덱싱 대상이 아니므로 그냥 물러납니다 — 이전에는 그때도 외장하드를 전부 훑었습니다.

**검증:** 재등록한 뒤 하드를 뽑았다 꽂아 확인했습니다. 등록된 정의에 `S4U`와 값 쿼리가 들어갔고, 그 실행이 남긴 로그는 꽂은 하드 한 줄이 전부였습니다.

```
[12:49:17] Works F (I:) 스캔 시작                  ← 재등록 전: 세 하드 전부
[12:49:17] Works E (J:) 스캔 시작
[12:49:23] Works D (L:) 스캔 시작

[12:51:03] Works D (L): 방금 스캔했으므로 건너뜀    ← 재등록 후: 이 한 줄뿐
```

Works E와 F는 언급조차 되지 않았습니다. 스케줄러가 이벤트에서 `L:`을 꺼내 넘긴 것이 맞습니다. 그 실행은 쿨다운(120초)에 걸려 스캔까지 가지 않았지만, 확인하려던 것은 어느 하드를 대상으로 삼는가였고 그것이 증명되었습니다.

### 마운트 이벤트 트리거 발화 확인 (2026-08-29)

마지막까지 남아 있던 미검증 항목입니다. 하드를 실제로 뽑았다 꽂아 확인했습니다. 시각이 그대로 이어집니다.

| 시각 | 사건 |
|---|---|
| 12:23:52 | System 로그에 `Microsoft-Windows-Ntfs` 이벤트 98 기록 |
| 12:24:08 | 작업 스케줄러가 `drive-archive sync` 실행 |

두 시각의 차이 16초는 작업 정의에 걸어 둔 `<Delay>PT15S</Delay>`와 맞아떨어집니다. 이번에는 `schtasks /Run`을 부르지 않았으므로, 하드를 꽂은 것만으로 작업이 깨어난 것이 맞습니다. **이벤트 발생 → 트리거 발화 → 스캔 실행**의 전 경로가 확인되었고, 상주 프로세스 없이 하드 연결을 잡아낸다는 설계 전제가 성립합니다.

같은 실행에서 diff도 함께 검증되었습니다. Works E는 171,139개가 그대로라고 정확히 판정했고, Works D에서는 지난 스캔 이후 늘어난 파일 하나를 잡아냈습니다(22,661 → 22,662).

**성능:** Works E의 171,139개를 훑는 데 **116초**가 걸렸습니다. 복사 직후 측정한 41초보다 오래 걸렸는데, 그때는 Windows 파일 캐시가 따뜻했고 이번에는 하드를 막 다시 꽂은 콜드 상태였기 때문으로 보입니다. 실사용에 가까운 값은 116초 쪽입니다.

### v0.2.1 — 등록 직후 바로 인덱싱, 검색 안내 정확도 (2026-08-28)

설치를 끝냈는데도 **이미 꽂혀 있던 하드가 인덱싱되지 않는** 결함을 고쳤습니다. `install`은 작업을 등록만 하고 끝났고, 트리거는 마운트 이벤트와 로그온뿐이라, 등록 시점에 이미 연결되어 있던 하드는 그 이벤트를 놓친 뒤였습니다. 다음 로그온까지 아무 일도 일어나지 않았고, 사용자가 하드를 뽑았다 다시 꽂아야 했습니다.

`task::run_now()`를 추가해 `install`과 `setup-task`가 등록 직후 `schtasks /Run`으로 작업을 한 번 깨웁니다. 실행에 실패해도 등록은 이미 끝났으므로 설치 자체는 성공으로 둡니다.

직접 `sync_all()`을 부르지 않은 이유는 세 가지입니다. `install`은 UAC로 상승된 별도 콘솔에서 이어지므로 거기서 스캔하면 그 창이 끝날 때까지 붙잡힙니다. 작업은 `LeastPrivilege`로 등록되어 있어 `schtasks /Run`을 타면 평소 자동 실행과 같은 일반 권한 경로로 돕니다. 중복 실행은 `MultipleInstancesPolicy=IgnoreNew`와 `InstanceLock`이 이미 막고 있습니다.

**검색 안내도 함께 고쳤습니다.** 검색 결과 맨 아래가 하드의 연결 여부를 보지 않고 무조건 "→ Works D 하드를 연결하세요"라고 안내했습니다. 이미 꽂혀 있는 하드를 서랍에서 헛되이 찾게 만드는 안내였습니다. `split_by_connection`을 두어 꺼내 와야 하는 하드와 이미 연결된 하드를 나눠 알리고, 연결된 쪽에는 드라이브 문자를 덧붙입니다.

판별은 라벨이 아니라 **볼륨 시리얼**로 합니다. 하드를 포맷하면 라벨은 그대로여도 시리얼이 바뀌므로, 라벨로 맞추면 인덱스에 남은 옛 항목을 새 볼륨과 같은 하드로 착각합니다. 아래 Works F 사례가 정확히 그 경우였습니다. `list_drives`가 쓰던 판별 방식을 그대로 따랐습니다.

**검증:** `schtasks /Run`이 비관리자 셸에서도 동작하고 작업이 실제로 깨어나 스캔을 시작하는 것을 실기로 확인했습니다. 빌드한 exe에 새 경로가 들어갔는지는 바이너리 문자열로 대조했습니다. 검색 안내는 연결된 하드로 실제 검색해 확인했고, 연결 여부·포맷된 하드·중복 결과를 덮는 단위 테스트 4개를 더했습니다. 테스트 75개 통과.

**함께 확인한 것:** 스캔 도중 프로세스를 강제 종료해도 인덱스가 온전히 남는 것을 실제로 확인했습니다. `sync_volume`이 스캔을 끝까지 마친 뒤에만 `apply_scan`을 호출하기 때문입니다.

### v0.2.0 — 모든 파일 시스템 지원 (2026-08-26)

파일 시스템 제한을 없앴습니다. 지원 목록을 늘리는 대신 **검사 자체를 제거**해서, Windows가 드라이브 문자를 붙여 준 볼륨이면 무엇이든 인덱싱합니다. HFS+처럼 별도 드라이버가 필요한 형식도 드라이버만 설치하면 코드 변경 없이 잡힙니다.

하드별 포맷을 인덱스에 기록하고 `drives`·`list_drives`에 표시합니다. macOS가 만드는 폴더(`.Spotlight-V100`, `.Trashes` 등)를 제외 목록에 추가했습니다.

**성과:** 그동안 필터에 걸려 통째로 빠져 있던 exFAT 하드(3.6TB)에서 171,139개 항목을 새로 인덱싱했습니다. 기존 NTFS 인덱스 22,661개는 마이그레이션에서 그대로 살아남았습니다.

**바로잡은 것:** "Ntfs 이벤트 98은 NTFS 전용이라 exFAT은 트리거가 안 될 것"이라고 판단해 주석에 적었으나, 이벤트 로그를 확인하니 exFAT 볼륨에서도 발생하고 있었습니다. 추측이 틀려 실측대로 고쳤습니다.

### v0.1.1 — 인덱스 보호 안전장치 (2026-08-26)

스캔 도중 하드가 분리되면 읽다 만 결과가 반영되어 멀쩡한 인덱스가 통째로 지워지는 결함이 있었습니다. 없는 드라이브를 스캔하면 `entries=0, errors=1`로 **정상 반환**되고, 그 결과가 diff에 들어가면 전량 삭제로 이어졌습니다.

세 겹으로 막았습니다.

1. 드라이브 루트를 읽지 못하면 빈 결과가 아니라 실패를 반환 (`scan.rs`)
2. 스캔 후 같은 하드인지 볼륨 시리얼로 재확인 (`volume.rs`의 `still_connected`)
3. 읽기 오류가 있으면서 항목이 이전의 절반 아래로 줄면 반영 보류 (`sync.rs`의 `is_scan_trustworthy`)

오류 없이 끝난 스캔은 결과가 얼마나 줄었든 그대로 믿습니다. 사용자가 정말 하드를 비웠다면 인덱스도 비워지는 것이 맞기 때문입니다.

`SyncOutcome`을 enum으로 바꿔 실패가 화면에 드러나게 했습니다. 이전에는 `sync_all`이 실패를 로그에만 남기고 삼켰습니다.

### v0.1.0 — 첫 릴리스 (2026-08-26)

인덱싱·검색·이벤트 트리거·MCP 서버·원커맨드 설치를 모두 구현했습니다.

**실기 검증:** 1.8TB 하드의 22,661개 항목을 4.8초에 인덱싱. 재스캔 시 diff가 "변경 없음"으로 정확히 판정. Claude Code MCP 클라이언트 연결 성공.

---

## 기술 결정 기록

같은 판단을 다시 하지 않도록, 왜 그렇게 했는지 남깁니다.

### MCP를 직접 구현했습니다 (rmcp 안 씀)

`rmcp` 크레이트를 실제로 넣어 보니 의존성이 81개에서 **127개로 늘고 tokio 런타임이 딸려 왔습니다.** MCP stdio는 줄 단위 JSON-RPC 2.0이 전부이고 읽기 전용 도구가 셋뿐이라, 이미 있던 `serde_json`만으로 약 200줄에 구현했습니다. 프로토콜 정확성은 실제 Claude Code 연결로 검증했습니다.

"굉장히 작은 리소스로 동작"이 이 프로그램의 핵심 요구사항이라, 비동기 런타임을 요청-응답 루프 하나 때문에 넣는 것은 맞지 않다고 판단했습니다.

### 상주 프로세스를 두지 않습니다

`WM_DEVICECHANGE`를 받으려면 프로세스가 항상 떠 있어야 합니다. 대신 Windows 작업 스케줄러의 **이벤트 트리거**를 씁니다. 평소 메모리·CPU 사용량이 0이고, 하드가 연결될 때만 잠깐 실행되고 끝나면 완전히 종료됩니다. 대가는 연결 후 스캔 시작까지 약 15초 지연입니다.

### 하드는 볼륨 시리얼로 식별합니다

드라이브 문자(`E:`, `F:`)는 연결 순서에 따라 바뀝니다. 인덱스에는 볼륨 시리얼만 저장하고, 경로도 드라이브 루트 기준 상대 경로로 넣습니다. 라벨은 사람이 물리 라벨과 대조하는 이름으로만 씁니다.

### 검색은 `LIKE`, FTS 아님

한글 부분 문자열 검색에는 `LIKE '%키워드%'`가 가장 정확합니다. 형태소 분석이 없는 FTS는 한글을 제대로 쪼개지 못합니다. 193,800행에서 체감 지연이 없어 v1에는 충분합니다.

### 파일 내용은 저장하지 않습니다

이름·경로·크기·수정 시각만 기록합니다. 인덱스가 가볍게 유지되고(193,800개 항목에 62.9MB), 파일 내용이 어딘가로 복사되는 일이 없습니다. 대신 파일 안의 내용은 검색할 수 없습니다.

---

## 확인된 환경 제약

- **개발 세션의 셸은 비관리자 토큰으로 실행됩니다.** 계정(`DESKTOP-C6UQNSE\JohnHB`)은 Administrators 소속이지만 UAC로 걸러져(`Group used for deny only`, Medium 무결성) 작업 스케줄러 **등록**이 거부됩니다. 단순한 `/SC ONLOGON` 작업 생성조차 `Access is denied`입니다.
- **다만 이미 등록된 작업을 다루는 것은 비관리자로도 됩니다.** `schtasks /Run`, `/End`, `/Change /ENABLE|/DISABLE` 모두 관리자 권한 없이 동작합니다(2026-08-28 확인). 작업 소유자가 본인이기 때문입니다. `run_now`가 성립하는 근거이자, 큰 작업 전에 자동 인덱싱을 잠시 꺼 둘 수 있는 근거입니다.
- **`target/release/drive-archive.exe`는 MCP 서버가 붙잡고 있으면 빌드로 덮어쓸 수 없습니다.** Claude 세션마다 `drive-archive.exe mcp` 프로세스가 하나씩 떠 있어, 여러 세션이 열려 있으면 `cargo build --release`가 `os error 5`로 실패합니다. 빌드 전에 `Get-Process drive-archive | Stop-Process -Force`로 정리하면 됩니다. 다음 요청 때 Claude가 알아서 다시 띄웁니다.
- **이 컴퓨터에는 HFS/APFS 드라이버가 없습니다.** Windows 기본 드라이버는 NTFS·exFAT·FAT32·ReFS·UDF뿐입니다. Mac 포맷 하드는 드라이브 문자조차 뜨지 않습니다.
- **빌드 도구:** Rust 1.95 (MSVC), Visual Studio 2019 Build Tools (MSVC 14.29).

---

## 이 문서 관리 규칙

기능을 추가하거나 결함을 고치거나 릴리스할 때 이 문서를 함께 갱신합니다.

- **최종 갱신·버전·상태**를 맨 위에서 고칩니다
- 숫자(항목 수, 테스트 수, 코드 줄 수)는 **추측하지 말고 실제로 측정한 값**을 넣습니다
- 미검증 항목을 검증했으면 [남은 일](#남은-일)에서 지우고 결과를 [개발 이력](#개발-이력)에 적습니다
- 판단이 갈렸던 선택은 [기술 결정 기록](#기술-결정-기록)에 왜 그렇게 했는지 남깁니다
